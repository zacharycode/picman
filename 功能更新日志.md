# Picman 功能更新日志

本文件是 Picman 唯一的功能更新日志。后续每一次功能更新都只记录在这里，按日期由近到远排列，并全部使用中文书写。

## 2026-08-02

- 图片浏览、缩略图与 macOS 体验优化（`v0.1.97`）：
  - 优化虚拟图片网格与快速滚动预渲染，优先加载可视区域，减少占位图闪烁和大资源库滚动卡顿。
  - 新增素材目录图片自动生成缩略图，修复扩展名与真实图片格式不一致时生成失败的问题，并支持失败统计、Finder 定位和重新生成。
  - 缩略图缓存改为不限容量的标准均衡品质，移除缓存上限与品质滑条，保留真实占用统计和手动清理。
  - 单张详情、灯箱和多选素材支持 macOS 原生分享菜单，可使用 AirDrop 等系统分享服务。
  - 右侧详情栏内容按自身宽度自适应；拖动或隐藏详情栏时，图片网格在绘制前同步重排，不再留下空白区域。
  - 修复冷启动白框和窗口缩放白边：首屏同步完成后再显示窗口，并让原生窗口、WebView 与页面背景保持一致。
  - 修正 macOS 红绿灯按钮位置和设置侧栏横向滚动问题，并完善窗口与分栏拖动的帧合并更新。
  - 验证结果：前端 28 项测试、Rust 23 项测试和 83 项性能审计通过；lint、生产构建与 macOS 实机验证通过。

## 2026-07-26

- 文件优先架构、缩略图管理与大资源库性能收口（`v0.1.96`）：
  - 将图片本体继续作为资源库唯一不可替代的数据源；目录索引、缩略图和运行缓存均为可重建、可查看、可清理的派生数据，不改变、不搬运原始图片。
  - 应用设置改为原生 JSON 文件持久化，覆盖最近资源库、窗口布局、分栏尺寸、浏览位置、视图、排序、主题、快捷键和缩略图设置；旧版浏览器存储会自动迁移，并对高频写入进行合并，避免持续触发磁盘写入。
  - 新增 `.picman/cache/catalog.jsonl` 本地目录索引，打开资源库时先恢复目录快照，再以真实文件扫描结果校准；设置面板可查看索引记录数、占用空间和有效性，并可单独重建或清理索引。
  - 新增原生文件系统监听，外部新增、删除、移动图片或修改文件夹元数据后自动合并刷新；监听会忽略 `.picman/cache`，避免缓存更新造成重复扫描。
  - 缩略图管理改为真实磁盘统计和真实容量上限，支持全库或指定文件夹生成、生成时选择质量、覆盖旧品质、实时进度、压缩现有缩略图和一键清理；超过限制时按最久未使用顺序回收缓存。
  - 完善开放文件协议：资源库设置位于 `.picman/settings.json`，文件夹及其图片的附加信息集中在 `.picman.folder.json`；读写会保留未知字段，便于未来 Mac、Web、iOS 与 Dropbox 直接同步同一文件库。
  - 快速滚动时保留已解码图片，并暂缓新进入视窗图片的解码，滚动稳定后再补齐内容；降低大量素材快速滑动时的主线程和图片解码压力，同时保持滚动条位移与输入一致。
  - 拆分资源库模型、设置面板样式、应用设置、目录索引、文件监听和缩略图缓存模块，补充中文架构、性能及文件格式文档，降低后续迭代时的耦合风险。
  - 新增 Vitest、组件测试、Rust 单元测试、自动化性能审计、2.5 万素材压力测试及 GitHub 持续集成和发布工作流。
  - 2.5 万素材基准资源库共 150 个目录、约 779 MB：真实原生扫描约 737 毫秒，目录索引恢复约 218 毫秒，80 项性能审计全部通过。
  - 验证结果：前端 12 项测试全部通过，Rust 20 项测试全部通过；静态检查、生产构建、2.5 万素材审计、Apple Silicon 桌面打包和更新器签名均通过。
  - 已生成 `Picman_0.1.96_aarch64.dmg` 与 `Picman.app.tar.gz`；应用为原生 Apple Silicon `arm64` 版本，更新器制品带独立签名。

## 2026-07-21

- 资源库批量移除数组预分配优化：
  - 将 Picman 版本更新到 `0.1.95`。
  - 批量删除、移动、批量处理或旋转后从资源库移除旧素材时，`removeAssetsFromLibrary` 不再用 `nextAssets.push(...)` 和 `removedAssets.push(...)` 动态增长数组。
  - 保留素材数组按资源库当前长度预分配，移除素材数组按移除数量预分配，并在单次循环后裁剪到真实数量。
  - 大量素材被删除或替换时，可减少资源库状态重建阶段的动态数组增长开销，同时保留缩略图统计扣减、预览 URL 回收和选择清理语义。
  - 性能审计脚本强化资源库批量移除检查，防止后续退回到 `push` 或多轮 `filter/map` 路径。
  - 该优化只调整本机运行时资源库状态移除方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 回收站批量列表更新预分配优化：
  - 将 Picman 版本更新到 `0.1.94`。
  - 删除素材到回收站后，回收站列表不再通过 `[...created, ...current]` 把新旧列表 spread 合并，改为 `prependItems` 预分配目标数组并按索引写入。
  - 从回收站恢复素材后，回收站列表不再通过 `current.filter(...)` 全量过滤重建，改为 `removeItemsById` 懒复制并只在确实移除条目时生成新数组。
  - 恢复回收站素材时，新素材数组也从 `push` 动态增长改为按恢复结果数量预分配并裁剪到真实新增数量。
  - 在批量删除或批量恢复大量素材时，可减少回收站列表更新阶段的临时数组分配和动态增长开销。
  - 性能审计脚本新增回收站列表预分配更新检查，防止后续退回到 spread 或 filter 路径。
  - 该优化只调整本机运行时回收站列表状态更新方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 侧边栏文件夹搜索预分配优化：
  - 将 Picman 版本更新到 `0.1.93`。
  - 左侧侧边栏派生子文件夹列表时，不再通过 `folders.filter(...)` 创建临时数组，改为预分配数组并按索引写入。
  - 文件夹搜索输入时，不再通过 `allChildFolders.filter(...)` 构造匹配结果，改为预分配循环筛选并裁剪到真实匹配数量。
  - 拖拽调整文件夹顺序时，不再通过 `allChildFolders.map(...)` 生成路径数组，改为预分配路径数组并按索引写入。
  - 在文件夹数量较多、实时搜索文件夹或拖拽排序时，可减少侧边栏列表派生阶段的回调调用和临时数组分配。
  - 性能审计脚本新增侧边栏文件夹搜索预分配检查，防止后续退回到 `filter/map` 路径。
  - 该优化只调整本机运行时侧边栏文件夹列表派生方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 可见素材筛选结果预分配优化：
  - 将 Picman 版本更新到 `0.1.92`。
  - 搜索、标签、文件夹、类型或缩略图状态筛选素材时，`visibleAssetIds` 不再通过 `ids.push(...)` 动态增长筛选结果数组。
  - 筛选结果改为按当前排序素材数量预分配数组，匹配素材时按索引写入，循环结束后裁剪到真实匹配数量。
  - 在大资源库执行搜索、切换标签、切换文件夹、筛选未生成缩略图等场景中，可减少筛选阶段的动态数组增长开销。
  - 性能审计脚本新增可见素材筛选结果预分配检查，防止后续退回到 `ids.push` 路径。
  - 该优化只调整本机运行时可见素材 ID 列表构造方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 文件夹自定义排序预分配优化：
  - 将 Picman 版本更新到 `0.1.91`。
  - 左侧目录树应用自定义文件夹顺序时，不再通过 `order.map(...)` 建立排序表，也不再对文件夹列表执行两次 `filter(...)` 后用 spread 合并。
  - `applyFolderOrder` 改为循环建立排序表，并在一次文件夹遍历中拆分根目录和子目录，排序后用预分配数组合并结果。
  - 在文件夹数量较多且存在自定义排序时，可减少目录树渲染前的数据准备分配和遍历次数。
  - 性能审计脚本新增文件夹排序预分配检查，防止后续退回到 `map/filter/spread` 路径。
  - 该优化只调整本机运行时文件夹列表排序方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 目录统计构建预分配优化：
  - 将 Picman 版本更新到 `0.1.90`。
  - 构建资源库目录统计时，目录树和缩略图文件夹列表不再分别通过 `folderItems.map(...)` 与 `filter(...).map(...)` 生成。
  - `buildLibraryCatalogState` 改为在一次文件夹条目循环里预分配并写入 `folders` 与 `thumbnailFolders`，标签列表也在过滤有效标签时同步写入预分配数组。
  - 打开资源库、刷新资源库、批量删除或移动后重建目录统计时，可减少重复遍历和临时数组分配。
  - 性能审计脚本新增目录统计构建预分配检查，防止后续退回到重复 `map/filter` 派生路径。
  - 该优化只调整本机运行时目录统计数据构造方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 刷新扫描合并结果数组预分配优化：
  - 将 Picman 版本更新到 `0.1.89`。
  - 刷新资源库后合并扫描结果时，`mergeRefreshedAssets` 不再通过 `assets.push(...)` 动态增长全量素材结果数组。
  - 合并结果改为按扫描素材数量预分配数组，并在单向循环中按索引写入保留缩略图信息后的素材对象。
  - 在大资源库刷新路径中，可减少全量合并阶段的动态数组增长开销，同时保持新增统计、删除统计、缩略图继承和选中集合清理语义不变。
  - 性能审计脚本强化刷新合并检查，要求结果数组预分配并禁止退回 `assets.push`。
  - 该优化只调整本机运行时刷新合并数组构造方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 文件夹计数局部更新懒复制优化：
  - 将 Picman 版本更新到 `0.1.88`。
  - 扫描批次追加素材后，更新左侧目录树和缩略图文件夹列表计数时，不再通过 `folders.map(...)` 为整个文件夹列表创建新数组。
  - `patchFolderNodeCounts` 改为按索引循环检查触及的文件夹，只有计数真的变化时才懒复制数组并替换对应节点。
  - 在大资源库分批扫描且文件夹列表较多时，可减少每个批次目录计数刷新阶段的回调调用和不必要数组分配。
  - 性能审计脚本新增文件夹计数懒复制检查，防止后续退回到全量 `map` 更新路径。
  - 该优化只调整本机运行时目录计数 UI 状态更新方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 扫描批次转换预分配优化：
  - 将 Picman 版本更新到 `0.1.87`。
  - Tauri 原生扫描批次进入前端时，不再通过 `event.payload.assets.map(nativeAssetToFrontend)` 构造素材数组，改为按批次长度预分配数组并用索引循环写入。
  - 本地文件批次扫描补齐搜索文本时，不再通过 `batch.map(withAssetSearchText)` 构造新数组，改为同样的预分配循环。
  - 打开大资源目录时，每个扫描批次都能减少回调调用和临时数组增长路径，使批次进入状态更新前更轻。
  - 性能审计脚本新增扫描批次转换预分配检查，防止后续退回到批次 `map` 转换路径。
  - 该优化只调整本机运行时扫描批次转换方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

## 2026-07-17

- 实时素材数组与缩略图 sources 预分配优化：
  - 将 Picman 版本更新到 `0.1.86`。
  - `getLiveAssets` 不再通过 `sourceAssets.map(...)` 重建实时素材对象数组，改为按素材数量预分配数组并用索引循环写入。
  - 全量生成缩略图时，传给原生后端的 sources 不再先 `every` 检查再 `map` 构造，改为一次预分配循环完成源路径校验和 sources 写入。
  - 在刷新资源库、全量生成缩略图、清理缩略图缓存等大资源库操作中，可减少全量数组回调调用和重复遍历。
  - 性能审计脚本新增实时素材数组与原生缩略图 sources 预分配检查，防止后续退回到全量 `map` 或 `every + map` 路径。
  - 该优化只调整本机运行时数组派生方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 素材 ID 列表重建预分配优化：
  - 将 Picman 版本更新到 `0.1.85`。
  - 资源库打开、刷新合并、批量替换等需要从素材对象数组重建 ID 顺序列表的路径，不再通过 `assets.map(...)` 派生全量 ID。
  - `assetIdsFromAssets` 改为按素材数量预分配字符串数组，并用索引循环写入素材 ID，减少大资源库状态重建时的回调调用和临时分配。
  - 性能审计脚本新增素材 ID 列表预分配检查，防止后续退回到全量 `map` 派生路径。
  - 该优化只调整本机运行时素材 ID 列表生成方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 选中集合清理直接循环优化：
  - 将 Picman 版本更新到 `0.1.84`。
  - 刷新资源库合并扫描结果后，保留仍存在的选中素材时不再通过 `[...currentSelectedIds].filter(...)` 展开整份选择集。
  - 恢复回收站素材后，清理回收站选中集合时复用 `removeIdsFromSet` 直接循环删除，不再先展开 Set 再过滤。
  - 大选择集场景下，刷新资源库或恢复回收站后可减少临时数组分配和一次额外集合重建。
  - 性能审计脚本新增选中集合清理直接循环检查，防止后续退回到 Set 展开过滤路径。
  - 该优化只调整本机运行时选中集合清理方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 键盘导航复用可见索引缓存优化：
  - 将 Picman 版本更新到 `0.1.83`。
  - 由于自适应、列表和当前瀑布流视图的视觉顺序都与 `visibleAssetIds` 一致，移除 App 层独立的 `visualOrderRef` 缓存。
  - 键盘方向键导航、跳到顶部/底部现在直接复用 `visibleAssetIds` 和现有 `getVisibleAssetIndex` 可见素材索引缓存。
  - 移除 `LibraryView` 向 App 同步视觉顺序的 `onVisualOrderChange` 回调和 effect，避免大资源库下为同一顺序维护第二套索引 Map。
  - 点击取消多选时不再通过 `Array.from(next)` 查找最后一个选中项，改为直接遍历 Set 得到最后 ID。
  - Shift 范围选择不再先 `visibleAssetIds.slice(...)` 再构造 Set，改为按可见 ID 区间直接循环写入目标集合。
  - 性能审计脚本新增键盘导航复用可见索引、点击多选和范围选择直接循环检查，防止后续重新引入重复视觉顺序缓存或大选择集临时数组。
  - 该优化只调整本机运行时键盘导航索引缓存路径，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 瀑布流比例字符串延迟生成优化：
  - 将 Picman 版本更新到 `0.1.82`。
  - 瀑布流全量布局阶段不再为每张素材生成 `String(ratio)` 比例字符串，而是保存数值比例。
  - 只有素材进入当前虚拟可见窗口、需要传给 CSS 变量 `--asset-aspect-ratio` 时，才把比例数值转换成字符串。
  - 在大资源库切换瀑布流、调整缩略图大小或窗口宽度变化时，可减少全量布局阶段的字符串分配。
  - 性能审计脚本新增比例字符串延迟生成检查，防止后续退回到布局阶段为全量素材生成比例字符串。
  - 该优化只调整本机运行时瀑布流布局数据字段，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 瀑布流虚拟窗口分配范围优化：
  - 将 Picman 版本更新到 `0.1.81`。
  - 瀑布流虚拟滚动在生成当前可见素材项前，新增 `upperBound` 二分定位可扫描窗口的结束位置。
  - 可见项数组不再按 `layoutData.items.length - startIndex` 这种接近全量剩余素材的长度预分配，而是按 `endIndex - startIndex` 的真实窗口范围预分配。
  - 在 25000 张级别资源库中位于列表顶部或中段快速滚动时，可显著减少每次虚拟窗口更新产生的大空数组和裁剪成本。
  - 性能审计脚本强化三种视图虚拟项预分配检查，防止瀑布流退回到按全量剩余素材分配。
  - 该优化只调整本机运行时虚拟滚动窗口数组分配方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 瀑布流视觉顺序复用优化：
  - 将 Picman 版本更新到 `0.1.80`。
  - 瀑布流布局已经按视觉顺序生成渲染项，因此不再额外为 `MasonryLayoutData` 分配 `visualIds` 全量 ID 数组。
  - 视觉顺序直接复用当前 `visibleAssetIds`，避免大资源库切换瀑布流或调整缩略图大小时复制一份 25000 级别的素材 ID 列表。
  - 性能审计脚本新增复用 `visibleAssetIds` 的检查，防止后续退回到瀑布流布局阶段重复生成 `visualIds`。
  - 该优化只调整本机运行时瀑布流视觉顺序数据复用方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 瀑布流位置索引懒生成优化：
  - 将 Picman 版本更新到 `0.1.79`。
  - 瀑布流布局阶段不再为全部素材立即建立 `positionById` 位置 Map。
  - 键盘滚动定位在目标素材不在当前视窗中时，才会通过 `WeakMap` 为当前瀑布流布局懒生成一次位置索引。
  - 普通浏览、滚动、切换瀑布流、调整缩略图大小或窗口宽度变化时，可减少全量素材位置 Map 写入和小对象分配。
  - 性能审计脚本新增瀑布流位置索引懒生成检查，防止后续退回到布局阶段 eager 建立 `positionById`。
  - 该优化只调整本机运行时瀑布流键盘滚动定位数据结构，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 瀑布流布局单次生成视觉顺序：
  - 将 Picman 版本更新到 `0.1.78`。
  - 瀑布流布局不再先按列暂存素材、再额外归并多列来生成视觉顺序。
  - 由于最短列布局每次都会选择当前最小 `top` 的列，且相同 `top` 时按左到右扫描列，布局生成顺序本身就是视觉顺序。
  - 现在布局循环会直接预分配 `items` 和 `visualIds`，按索引同步写入，减少大资源库切换瀑布流或调整缩略图大小时的一整轮列归并扫描。
  - 性能审计脚本新增单次生成视觉顺序检查，防止后续退回到 `columnItems` 暂存或 `mergeMasonryColumns` 归并路径。
  - 该优化只调整本机运行时瀑布流布局计算方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 素材 ID 排序数组预分配优化：
  - 将 Picman 版本更新到 `0.1.77`。
  - `sortAssetIds` 继续在排序前预取素材引用，避免排序比较函数中反复查询 `Map`。
  - 素材 ID 排序从 `map -> sort -> map` 改为预分配引用数组、按索引写入、排序后预分配结果 ID 数组。
  - 在大资源库打开、切换排序、筛选复用排序顺序时，可减少排序准备阶段和结果回收阶段的回调调用与临时数组分配。
  - 性能审计脚本新增排序数组预分配检查，防止后续退回到两端 `map` 的实现。
  - 该优化只调整本机运行时排序数组构建方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 瀑布流归并数组预分配优化：
  - 将 Picman 版本更新到 `0.1.76`。
  - 瀑布流在把多列布局归并成视觉顺序时，改为按素材总量预分配布局项数组和视觉 ID 数组，并按输出索引写入。
  - 归并完成后会裁剪到实际输出长度，避免大资源库切换瀑布流或调整缩略图大小时在全量归并阶段使用 `push` 动态增长数组。
  - 性能审计脚本强化瀑布流列归并检查，要求结果数组预分配、按索引写入，并防止退回 `items.push` / `visualIds.push`。
  - 该优化只调整本机运行时瀑布流布局结果数组构建方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 重新打包发布当前测试版本：
  - 将 Picman 版本更新到 `0.1.75`。
  - 基于当前工作区状态重新生成 macOS Apple Silicon 安装包和 Tauri 更新包，并准备上传到 GitHub Release 供外部测试机下载。
  - 本次只更新发布版本号与安装包产物，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build`、`npm run release:prepare` 验证，并已发布 GitHub Release `v0.1.75`。

- 虚拟滚动可见项数组预分配优化：
  - 将 Picman 版本更新到 `0.1.74`。
  - 自适应视图、列表视图和瀑布流视图在计算当前可见素材项时，改为预分配可见项数组并按索引写入。
  - 自适应和列表视图会按可见范围精确分配数组长度；瀑布流视图会按可扫描上限分配后裁剪到实际可见数量。
  - 快速滚动时，可减少 `items.push` 带来的动态数组增长和函数调用开销，让虚拟窗口更新更轻。
  - 性能审计脚本新增三种视图可见项预分配检查，防止后续迭代退回到 `push` 动态增长路径。
  - 该优化只调整本机运行时虚拟滚动可见项数组构建方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 瀑布流视觉顺序归并优化：
  - 将 Picman 版本更新到 `0.1.73`。
  - 瀑布流按列归并时，现在会同时生成布局项数组和视觉顺序 ID 数组，不再先得到排序后的布局项，再额外 `map` 一遍生成 `visualIds`。
  - 瀑布流总高度改为在布局循环中同步维护最大列高，不再使用 `Math.max(...columnHeights)` 展开列高数组。
  - 在 25000 张级别资源库中切换瀑布流、调整缩略图大小或窗口宽度变化时，可减少一次全量遍历和一次数组参数展开。
  - 性能审计脚本新增视觉顺序同步归并和最大列高增量维护检查，防止后续迭代退回到额外 `map` 或 spread。
  - 该优化只调整本机运行时瀑布流布局计算方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 瀑布流缩略图比例渲染优化：
  - 将 Picman 版本更新到 `0.1.72`。
  - 瀑布流布局阶段已经计算出的素材比例，现在会随虚拟项布局数字一起传给 `AssetItem`。
  - `AssetItem` 不再为了瀑布流缩略图重复解析素材尺寸字符串，也不再为缩略图容器单独创建 `aspectRatio` style 对象。
  - 缩略图比例改由根节点 CSS 变量 `--asset-aspect-ratio` 承接，CSS 中的瀑布流缩略图容器直接读取该变量。
  - 快速滚动瀑布流时，可减少可见素材项渲染阶段的重复尺寸解析和临时对象分配。
  - 性能审计脚本新增瀑布流比例复用与 CSS 变量渲染检查，防止后续迭代退回到素材项内重复计算比例。
  - 该优化只调整本机运行时瀑布流渲染数据传递方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 素材项布局比较优化：
  - 将 Picman 版本更新到 `0.1.71`。
  - 素材墙虚拟项不再由父组件生成 CSS style 对象后传入 `AssetItem`，改为直接传入固定字段的布局数字。
  - `AssetItem` 的 memo 比较从 `Object.keys` 式 style 深比较改为固定字段比较，只检查 `height`、`left`、`right`、`top`、`width`。
  - 快速滚动或缩略图批量刷新时，可减少可见素材项 memo 判断阶段的对象枚举成本，让渲染链路更轻。
  - 样式对象仍只在素材项真正渲染时生成，列表、自适应和瀑布流的定位语义保持不变。
  - 性能审计脚本新增固定字段布局比较检查，防止后续迭代退回到 `Object.keys` 比较 style 对象。
  - 该优化只调整本机运行时虚拟项渲染与 memo 比较方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 自适应与列表虚拟项对象分配优化：
  - 将 Picman 版本更新到 `0.1.70`。
  - 自适应视图和列表视图的虚拟滚动项不再在布局计算阶段创建 CSS style 对象，改为只保存素材 ID、位置、高度、宽度等数值。
  - 三种视图现在统一为“布局数据数值化，可见项渲染时再生成样式”的模式，减少快速滚动和视图切换时的短命对象分配。
  - 列表视图继续保留横向填满容器的定位语义，自适应视图继续保留原有列宽、间距和滚动定位逻辑。
  - 性能审计脚本新增自适应/列表虚拟项数值化检查，防止后续迭代退回到布局阶段提前创建 style 对象。
  - 该优化只调整本机运行时虚拟滚动布局数据结构，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 缩略图清理与文件夹生成取数优化：
  - 将 Picman 版本更新到 `0.1.69`。
  - 重新生成缩略图前，旧缩略图状态、缓存统计扣减和 blob URL 回收统一由单次遍历的清理补丁完成，避免先构造目标 ID 集、再全库过滤、再生成更新补丁的多轮处理。
  - 按文件夹生成缩略图时，改为单次遍历资源库素材并按需取得 live 素材对象，不再先生成整库 live 数组后再过滤目标文件夹。
  - 清理全部缩略图缓存也复用同一清理补丁路径，减少缩略图状态清理逻辑分叉，降低后续迭代出错概率。
  - 该优化只调整本机运行时缩略图状态清理和目标素材收集方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build`、`npm run release:prepare` 验证，并已发布 GitHub Release `v0.1.69`。

- 批量结果插入资源库优化：
  - 将 Picman 版本更新到 `0.1.68`。
  - 新增批量素材插入路径，恢复回收站、批量调整/压缩、批量旋转生成的新素材会一次性写入资源库运行时状态。
  - 批量插入会一次性更新素材 Map、目录/标签统计、素材 ID 列表和主素材数组，不再对每个新素材逐个调用 `insertCollectedAsset` 触发多组 React 状态更新。
  - 单张收藏仍保留原入口，但内部复用同一批量插入逻辑，避免单张和批量路径后续行为漂移。
  - 在恢复或批量处理大量素材时，可减少操作完成后的状态更新次数和渲染压力，让界面更快回到可操作状态。
  - 性能审计脚本新增批量插入检查，防止后续迭代退回到逐个素材插入。
  - 该优化只调整本机运行时资源库状态写入方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 大选择集手势操作优化：
  - 将 Picman 版本更新到 `0.1.67`。
  - 拖拽导出素材时，不再先把 `visibleSelectedIds` 展开成数组再 `map/filter` 查找源文件路径，改为直接遍历选择集收集可拖拽文件路径。
  - 批量调整/压缩弹窗计算可处理素材数量时，不再通过 `[...visibleSelectedIds].filter(...)` 创建临时数组，改为直接循环计数。
  - 在 `⌘A` 或大量多选后进行拖拽、打开批量弹窗等手势操作时，可减少一次性数组展开和重复过滤，让操作触发更轻。
  - 性能审计脚本新增拖拽导出与批量弹窗计数的直接循环检查，防止后续迭代退回到大选择集数组展开。
  - 该优化只调整本机运行时交互数据收集方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 移动与替换素材归并性能优化：
  - 将 Picman 版本更新到 `0.1.66`。
  - 移动素材时复用目标收集阶段生成的相对路径映射，后端返回移动结果后不再对目标列表逐条 `find` 旧素材。
  - 文件夹改名后的素材 remap 归并改为单次建立 live 素材路径索引，再循环生成替换列表，不再通过 `getLiveAssets(...).map(...)` 生成中间数组。
  - `replaceLibraryAssets` 不再通过 `replacements.map`、全库 `filter` 和数组 spread 组合新素材数组；现在一次循环生成旧 ID、新素材和 ID 映射，再一次遍历资源库保留未替换素材并追加新素材。
  - 在移动或改名大量素材时，可减少结果归并阶段的重复查找、临时数组和大数组 spread，降低操作完成瞬间的卡顿风险。
  - 性能审计脚本新增移动/替换单次循环归并检查，防止后续迭代退回到逐条 `find` 或 `map/filter/spread`。
  - 该优化只调整本机运行时素材替换归并方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 批量操作目标收集与结果处理优化：
  - 将 Picman 版本更新到 `0.1.65`。
  - 删除到回收站、批量调整/压缩、批量旋转在收集目标素材时，会同时生成目标素材、相对路径列表、目标 ID 集和 `relativePath -> id` 映射。
  - 批量处理和旋转的后端结果返回后，直接通过相对路径映射找到需要移除的旧素材 ID，不再先构建 `processedPaths`，再对目标列表做 `filter + map`。
  - 新素材数组、选中新素材 ID、批量处理前后容量统计改为同一次结果循环中累积，不再额外 `map` 或 `reduce`。
  - 在一次性处理大量素材时，可减少前端目标准备和结果归并阶段的临时数组与重复遍历，让操作完成后的界面响应更平稳。
  - 性能审计脚本新增批量操作目标映射检查，防止后续迭代退回到多轮 `map/filter` 反查旧素材 ID。
  - 该优化只调整本机运行时批量操作数据归并方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 批量移除素材的单次遍历优化：
  - 将 Picman 版本更新到 `0.1.64`。
  - 删除到回收站、批量旋转、批量调整/压缩等路径最终从资源库移除旧素材时，不再先构造全量 live 素材数组，再分别过滤移除项和保留项。
  - `removeAssetsFromLibrary` 改为单次遍历资源库素材数组，同时累积保留素材、移除素材和待扣减的缩略图统计。
  - 已选素材集合移除被删除 ID 时改为 Set 循环按需复制，不再通过 `[...current].filter(...)` 先展开整份选择集。
  - 在一次性删除或批量替换大量素材时，可减少全库多轮遍历和临时数组分配，降低操作完成瞬间的卡顿风险。
  - 性能审计脚本新增批量移除单次遍历检查，防止后续迭代退回到 `getLiveAssets + filter + filter` 或选择集数组展开。
  - 该优化只调整本机运行时资源库状态更新方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 扫描期间目录统计局部更新优化：
  - 将 Picman 版本更新到 `0.1.63`。
  - 资源库 catalog 状态新增运行时 `folderCounts`，扫描批次追加时不再从左侧目录数组反向重建文件夹计数 Map。
  - 打开大资源目录过程中，如果当前批次没有引入新的文件夹或新的标签，则只局部更新已有文件夹计数、标签计数和原图容量，不再重排整个目录与标签列表。
  - 新文件夹或新标签出现时仍会完整构建目录/标签列表，保证左侧目录树、标签栏和缩略图生成文件夹列表的显示顺序正确。
  - 文件夹改名、标签编辑和资源库刷新继续使用完整构建或已有局部标签计数路径，行为保持不变。
  - 性能审计脚本新增运行时 `folderCounts` 和 catalog 局部计数更新检查，防止后续迭代退回到每个扫描批次都重建目录计数并排序。
  - 该优化只调整本机运行时 catalog 派生状态，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 打开资源库批次追加分配优化：
  - 将 Picman 版本更新到 `0.1.62`。
  - 打开大资源目录时，素材 ID 列表追加不再通过 `incoming.map` 和数组 spread 生成临时数组，改为预分配目标数组后用循环写入。
  - 主素材数组的批次追加也改为预分配循环，避免每个扫描批次都使用 `[...current, ...incoming]`。
  - 原生扫描事件收集刷新素材和待刷新增量素材时，不再使用 `push(...incoming)` 展开批次数组，改为显式循环追加，减少函数参数展开和潜在的大批次风险。
  - 在 25000 张级别资源库打开过程中，可减少每批次追加时的临时数组分配，让扫描期间的 UI 更新更平稳。
  - 性能审计脚本新增批次追加预分配检查，防止后续迭代退回到 `incoming.map`、数组 spread 或 `push(...incoming)`。
  - 该优化只调整本机运行时批次追加方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 刷新资源库合并性能优化：
  - 将 Picman 版本更新到 `0.1.61`。
  - 刷新资源库完成后，素材合并逻辑不再通过多次 `map`、`filter` 构建旧路径集合、新路径集合、ID 集、合并数组和移除数组。
  - 合并流程改为单向循环：一次建立旧素材索引和旧路径集合，一次处理新扫描素材并累积新增数量、ID 集与合并结果，一次找出已移除素材。
  - 刷新 25000 张级别资源库时，可减少多轮全量遍历和临时数组分配，降低刷新结束瞬间的主线程压力。
  - 原有缩略图状态继承、移除素材预览 URL 回收、新增/移除数量统计和选择保留逻辑保持不变。
  - 性能审计脚本新增刷新合并单向循环检查，防止后续迭代退回到多次 `map/filter` 分配。
  - 该优化只调整本机运行时刷新合并算法，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 大量选择后的筛选与操作性能优化：
  - 将 Picman 版本更新到 `0.1.60`。
  - 计算“当前可见的已选素材”时，会优先遍历更小的集合：筛选结果更少时遍历可见 ID 并与选择集取交集，避免仍扫描 25000 个已选 ID。
  - 选择集为空时直接复用空选择集，不再进入过滤判断。
  - 批量旋转所需的素材对象数组改为点击批量旋转时再生成，不再在每次 App 渲染阶段从所有可见选中 ID 重建大数组。
  - 在 `⌘A` 大量选中后继续搜索、切换标签/文件夹/筛选或普通刷新时，可减少无意义的 Set/数组构建和素材匹配判断。
  - 性能审计脚本新增可见选中项“小集合优先遍历”和批量旋转“操作触发时生成素材数组”的检查，防止后续迭代回退。
  - 该优化只调整本机运行时选择派生和操作触发逻辑，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 瀑布流布局对象分配优化：
  - 将 Picman 版本更新到 `0.1.59`。
  - 瀑布流全量布局阶段不再为每一个素材提前创建 React CSS style 对象。
  - 全量布局数据现在只保存素材 ID、位置、宽度和高度等数值信息；进入当前虚拟滚动可视窗口的素材才会生成 style。
  - 在 25000 张级别资源库中，切换瀑布流、调整缩略图大小或窗口宽度变化时，可减少大量临时对象分配和垃圾回收压力。
  - 视觉顺序归并、键盘导航定位、瀑布流总高度和可视窗口计算保持不变。
  - 性能审计脚本新增“瀑布流可视阶段才生成 style”的检查，防止后续迭代退回到全量预生成样式对象。
  - 该优化只调整本机运行时布局数据结构，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 分割线拖拽手感优化：
  - 将 Picman 版本更新到 `0.1.58`。
  - 新增高频数值状态提交工具，把分割线拖拽中的连续 `pointermove` 更新合并到 `requestAnimationFrame`。
  - 左侧栏宽度、右侧详情栏宽度、左侧文件夹与标签上下分割线，现在一帧最多提交一次 React 状态更新，并在松手或取消时补交最终位置。
  - 快速拖动分割线时，可减少主线程状态更新次数，降低布局抖动，让大资源库浏览时的窗口调整更顺滑。
  - 性能审计脚本新增分割线按帧提交检查，防止后续迭代退回到每个 `pointermove` 直接 `setState`。
  - 该优化只调整本机运行时交互节奏，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 重新打包测试版：
  - 将 Picman 版本更新到 `0.1.57`。
  - 基于当前本机最新代码重新生成 macOS Apple Silicon 安装包、更新包和 `latest.json` 清单。
  - 该更新只用于区分新的测试安装包，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run stress:audit`、`cargo check`、`npm run desktop:build` 与 `npm run release:prepare` 验证。

- 素材 ID 排序预取优化：
  - 将 Picman 版本更新到 `0.1.56`。
  - `sortAssetIds` 现在会在排序前把素材 ID 映射为 `{ id, asset }`，每个素材只查询一次运行时素材 Map。
  - 排序比较阶段直接比较已预取的素材引用，不再在每一次 comparator 调用中重复执行 `assetById.get`。
  - 名称、日期、大小、类型排序规则以及相对路径兜底规则保持不变；缺失素材 ID 的排序位置也保持原有语义。
  - 在 25000 张级别资源库中，切换排序方式、扫描结束排序或小结果筛选补排序时，可减少 `O(n log n)` 比较阶段的 Map 查询次数。
  - 性能审计脚本新增排序预取检查，防止后续迭代退回到排序比较阶段反复查 Map。
  - 该优化只调整本机运行时排序实现，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 运行时标签计数优化：
  - 将 Picman 版本更新到 `0.1.55`。
  - 资源库 catalog 状态新增运行时 `tagCounts`，标签集合不再只有标签名列表，而是同时维护每个标签的使用数量。
  - 添加或移除标签时，标签列表通过计数增减完成局部更新，不再扫描素材数组判断某个标签是否还存在。
  - 删除当前正在浏览的标签时，也改用 `tagCounts` 判断该标签删除后是否仍被其它素材使用，避免 25000 张级别资源库中的全库 `some` 扫描。
  - 文件夹数量、缩略图文件夹列表和原图容量继续复用现有 catalog 状态；标签计数只是本机运行时派生数据，不写入资源目录。
  - 性能审计脚本新增运行时标签计数检查，防止后续迭代退回到通过扫描素材数组维护标签存在性的方式。
  - 该优化只调整本机运行时 catalog 派生状态，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 标签统计局部更新优化：
  - 将 Picman 版本更新到 `0.1.54`。
  - 添加或移除素材标签时，不再为了刷新左侧标签列表而全量重建文件夹数量、标签集合和原图容量统计。
  - 元数据提交会识别“只改标签、文件夹和原图大小不变”的情况，复用现有文件夹统计和容量，只局部更新标签集合。
  - 删除标签时仍会按需检查该标签是否还被其它素材使用；添加标签时直接并入标签集合，避免无意义的全库目录统计派生。
  - `libraryAssets` 仍会在标签变更时更新，保证后续删除、移动、刷新等路径的数据一致；本次优化只减少目录/容量统计重算。
  - 性能审计脚本新增标签统计局部更新检查，防止后续迭代退回到标签增删时全量派生目录统计。
  - 该优化只调整本机运行时目录/标签统计派生策略，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 素材总数统计依赖优化：
  - 将 Picman 版本更新到 `0.1.53`。
  - 缩略图待生成数量和首次生成缩略图提示弹窗的素材总数，改为使用轻量的 `libraryAssetIds.length`。
  - 这些总数类 UI 不再依赖整张 `libraryAssets` 素材对象数组，继续沿用运行时维护的素材 ID 顺序列表。
  - 该调整延续“素材对象映射、ID 顺序列表、目录统计状态拆分”的性能方向，减少普通渲染对大对象数组状态的依赖。
  - 性能审计脚本新增总数来源检查，防止后续迭代让总数显示重新依赖 `libraryAssets.length`。
  - 该优化只调整本机运行时统计来源，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 删除标签按需扫描优化：
  - 将 Picman 版本更新到 `0.1.52`。
  - 删除素材标签时，不再无条件扫描整个资源库判断该标签是否还被其它素材使用。
  - 只有当前左侧正在浏览的标签正好是被删除的标签时，才会扫描全库判断是否需要自动回到“全部”。
  - 标签列表本身仍由后续目录/标签统计重建保证正确；普通删除非当前浏览标签时，省掉一次 25000 张级别资源库的全量 `some` 扫描。
  - 性能审计脚本新增删除标签按需扫描检查，防止后续迭代退回到每次删除标签都扫描全库。
  - 该优化只调整本机运行时标签编辑判断顺序，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 备注编辑素材数组复制优化：
  - 将 Picman 版本更新到 `0.1.51`。
  - 元数据提交路径现在会先判断是否影响目录统计，再决定是否复制整张 `libraryAssets` 数组。
  - 编辑备注这类只影响素材搜索文本和详情展示的字段时，只更新运行时素材映射和可见列表版本，不再复制 25000 项级别的素材数组。
  - 标签、文件夹或原图大小变化仍会更新 `libraryAssets` 数组并重建目录/标签/容量统计，保证左侧目录、标签筛选和容量显示正确。
  - 性能审计脚本新增“非统计元数据跳过素材数组复制与目录统计重建”的检查，防止后续迭代退回到先复制全量数组再判断的形式。
  - 该优化只调整本机运行时状态更新顺序，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 元数据编辑目录统计重建优化：
  - 将 Picman 版本更新到 `0.1.50`。
  - 编辑素材备注时，不再触发整个资源库的目录、标签和容量统计全量重建；备注只更新素材本身、搜索文本和可见列表版本。
  - 元数据提交会先比较旧素材与新素材的目录统计相关字段；只有标签、文件夹或原图大小变化时，才重新派生左侧目录和标签统计。
  - 标签新增/移除仍会正确更新标签列表，收藏状态仍只写回文件夹级元数据，不影响目录统计。
  - 在 25000 张级别资源库中，连续编辑备注或 OCR 后保存备注时，避免每次都扫描全库重算目录状态，减少右侧详情编辑后的卡顿感。
  - 性能审计脚本新增元数据目录统计重建保护检查，防止后续迭代让备注等非统计字段重新触发全量目录派生。
  - 该优化只调整本机运行时状态派生策略，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 可见素材筛选短路优化：
  - 将 Picman 版本更新到 `0.1.49`。
  - 中央素材列表的筛选函数从“先计算文件夹、标签、类型、缩略图和搜索五个条件再统一返回”改为逐项短路。
  - 切换文件夹时，不在当前文件夹的素材会立即跳过，不再继续执行标签数组匹配、缩略图状态判断和搜索字符串匹配。
  - 切换标签、类型或缩略图筛选时，同样会在更便宜的条件失败后提前返回，减少 25000 张级别资源库中每次筛选/搜索的无效计算。
  - 搜索仍优先使用素材的运行时预计算 `searchText`，兜底逻辑保持不变；行为结果不变，只降低无关素材的匹配成本。
  - 性能审计脚本新增筛选短路检查，防止后续迭代退回到全部条件都先计算的形式。
  - 该优化只调整本机运行时筛选逻辑，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 瀑布流布局排序优化：
  - 将 Picman 版本更新到 `0.1.48`。
  - 瀑布流全量布局时不再把所有素材布局项复制出来后做一次 `.sort()` 全量排序；现在每一列在布局生成过程中天然保持从上到下有序，再通过按列归并生成整体视觉顺序。
  - 视觉顺序仍保持从上到下、同高度从左到右，方向键导航和可见窗口二分查找继续使用同一份排序后的布局结果。
  - 在 25000 张级别资源库中，切换到瀑布流、调整缩略图大小或窗口宽度变化时，布局阶段避免 `O(n log n)` 全量排序，改为与列数相关的稳定归并，降低一次性计算峰值。
  - 性能审计脚本新增瀑布流按列归并检查，防止后续迭代退回到布局阶段全量 `.sort()`。
  - 该优化只调整本机运行时布局算法，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 虚拟滚动状态降频优化：
  - 将 Picman 版本更新到 `0.1.47`。
  - 中央素材视图的滚动测量仍保持 `requestAnimationFrame` 监听，但提交给 React 的虚拟滚动状态改为 48px 步进窗口，不再因为每 1px 滚动都触发虚拟列表重算。
  - 滚动速度从连续浮点值改为离散档位，动态预渲染范围仍会根据慢速、中速、高速滚动扩大，但不会因为速度数值的微小抖动造成额外渲染。
  - 真实滚动条位置、键盘滚动、滚动位置持久化和上下文切换恢复仍使用浏览器容器的真实 `scrollTop`，只降低虚拟布局计算频率，避免影响操作精度。
  - 性能审计脚本新增虚拟滚动窗口步进与速度档位检查，防止后续迭代退回到像素级滚动状态提交。
  - 该优化只调整本机运行时 UI 测量与渲染节奏，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 滚动渲染与滚动位置保存优化：
  - 将 Picman 版本更新到 `0.1.46`。
  - 素材卡片的 `memo` 从默认浅比较升级为自定义比较：滚动时虚拟列表会不断生成新的样式对象，但只要素材、选中状态、主选中状态、视图模式和样式值没有实际变化，已渲染的可见卡片就不再重新执行渲染。
  - 保留稳定事件桥接，点击、右键、双击和拖拽回调不会因为父组件刷新而迫使素材卡片重渲染，缩略图批量更新和高速滚动时的界面更轻。
  - 滚动位置持久化改为单定时器节流：连续滚动时只记录最新位置并复用同一个保存任务，避免每次滚动都反复清理和创建保存定时器，减少主线程小抖动。
  - 性能审计脚本新增自定义素材卡片 memo 与滚动位置单定时器节流检查，防止后续迭代退回到高频重渲染或高频定时器 churn。
  - 该优化只调整本机运行时 UI 渲染和设置保存节奏，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 键盘滚动定位的 DOM 查询优化：
  - 将 Picman 版本更新到 `0.1.45`。
  - 键盘选择素材后滚动定位时，不再通过 `querySelectorAll('.asset-item')` 取出全部可见节点再逐个查找目标素材。
  - 新增安全的 data 属性选择器转义，并改为直接使用目标素材 ID 查询对应 DOM 节点，减少方向键移动时的数组分配和可见节点遍历。
  - 未渲染到视窗中的素材仍继续走虚拟布局位置计算，键盘滚动到目标素材的行为保持不变。
  - 性能审计脚本新增直接查询目标 DOM 的检查，防止后续迭代退回到扫描全部可见节点。
  - 该优化只调整本机运行时 UI 查询方式，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 键盘连续导航的视觉顺序索引优化：
  - 将 Picman 版本更新到 `0.1.44`。
  - 中央素材视图的视觉顺序从单纯数组引用升级为懒加载索引缓存；普通渲染和点击仍只保存视觉顺序数组，只有连续方向键移动需要查找当前素材位置时才构建索引。
  - 方向键导航不再通过 `visualIds.indexOf(primaryId)` 每次线性查找当前素材，25000 张级别大资源库中按住方向键移动时，当前项定位会复用视觉顺序索引。
  - 视觉顺序缓存会绑定当前可见列表版本，切换搜索、筛选、视图模式或瀑布流布局后自动回退到新的可见顺序，避免使用过期索引。
  - 性能审计脚本新增视觉顺序懒索引检查，防止后续迭代退回到连续方向键移动时反复线性查找。
  - 该优化只调整本机运行时 UI 状态，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 可见列表索引按需构建：
  - 将 Picman 版本更新到 `0.1.43`。
  - 移除中央素材列表每次变化时立即构建 `visibleIdSet` 和 `visibleIndexById` 的逻辑，改为懒加载可见索引缓存；只有灯箱导航、Shift 连选或键盘滚动定位真正需要索引时才构建。
  - 当前选中素材集合改为按选中项检查是否仍匹配筛选条件，避免在 25000 张全量可见时为了普通单选/多选状态每次都扫描整张可见列表。
  - 右侧详情的主选中素材也改为直接判断该素材是否匹配当前筛选，不再依赖整张可见 Set。
  - 中央视图不再接收整张可见索引 Map，改为接收按需查询索引的函数，减少大列表变化时传递和构建大对象的成本。
  - 性能审计脚本新增懒可见索引检查，防止后续迭代退回到每次可见列表变化都生成 Set/Map。
  - 该优化只调整本机运行时 UI 状态，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 大列表筛选与搜索的排序复用：
  - 将 Picman 版本更新到 `0.1.42`。
  - 新增基于素材 ID 的排序工具，当前排序方式下的全库 ID 顺序会被复用；无筛选时直接返回已排序 ID，避免重复遍历素材对象。
  - 有文件夹、标签、类型、缩略图状态或搜索条件时，筛选逻辑沿着已排序 ID 顺序扫描并收集命中项，不再每次交互都先过滤对象再对结果重新排序，减少 25000 张级别资源库里搜索、切标签、切文件夹时的 `O(n log n)` 排序成本。
  - 打开资源目录扫描期间仍保留大结果延后排序策略；如果用户在扫描中启用筛选且结果较小，会继续对小结果排序，兼顾交互语义和流畅度。
  - 性能审计脚本新增筛选/搜索复用排序 ID 的检查，防止后续迭代退回到每次筛选重复排序。
  - 该优化只调整本机运行时 UI 状态，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 打开大资源库时的可见列表快速路径：
  - 将 Picman 版本更新到 `0.1.41`。
  - 新增运行时素材 ID 列表，并随扫描批次、收集助手追加、删除、移动、重命名和刷新结果同步维护。
  - 打开资源目录期间，如果当前没有文件夹、标签、类型、缩略图状态或搜索筛选，且素材数量超过大库阈值，中央素材列表直接复用增量 ID 列表，不再每个扫描批次都遍历当前全部素材来重新筛选和映射 ID。
  - 有筛选、搜索、缩略图状态筛选或扫描结束后的排序场景继续使用原筛选排序逻辑，保证功能语义不变。
  - 性能审计脚本新增无筛选打开扫描快速路径检查，防止后续迭代退回到扫描期重复全量映射。
  - 该优化只调整本机运行时 UI 状态，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 资源目录统计增量化优化：
  - 将 Picman 版本更新到 `0.1.40`。
  - 目录树、标签列表和资源库原图总容量从渲染期全量派生改为显式状态管理，避免大资源目录扫描时每追加一批素材都重新遍历当前全部素材。
  - 打开本地资源目录、浏览器预览目录和图片收集助手追加素材时，目录统计改为只合并新增素材批次；删除、移动、重命名、标签和备注修改等低频操作继续使用全量重建作为安全回退，确保计数、标签和容量不会漂移。
  - 资源库名称变化时只重算目录显示名称，不重扫图片列表，减少扫描启动和完成阶段的额外计算。
  - 性能审计脚本新增目录统计增量化检查，防止后续迭代退回到扫描期全量派生。
  - 该优化只调整本机运行时 UI 状态，不新增索引文件，不改变图片文件、缩略图缓存、文件夹级元数据或资源库设置结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check` 与 `npm run desktop:build` 验证。

- 大资源目录扫描期间的流畅度优化：
  - 将 Picman 版本更新到 `0.1.39`。
  - 打开本地资源目录时，前端扫描批次提交改为自适应节流：少量素材保持快速首屏反馈，批次密集涌入时自动降低 React 状态提交频率，待提交素材过多时再强制 flush，减少 25000 张级别大目录打开时的列表/目录统计重复计算。
  - 扫描进度文案增加轻量节流，避免原生扫描线程快速发送批次时每一批都触发一次界面重渲染。
  - 性能审计脚本新增扫描批次自适应节流检查，防止后续迭代把大库打开链路退化回高频提交。
  - 该优化只改变本机 UI 提交节奏，不改变图片文件、缩略图缓存、文件夹级元数据或资源库文件结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、本地浏览器预览刷新检查与 `npm run desktop:build` 验证。

- 素材墙渲染性能与瀑布流稳定性优化：
  - 将 Picman 版本更新到 `0.1.38`。
  - 素材仓库新增独立的布局版本号：只有素材尺寸、缩略图尺寸等会影响瀑布流高度的数据变化时，才触发布局重算；收藏、备注、标签、选区等非尺寸状态不再误触发瀑布流全量布局更新。
  - 中央视图素材项改为 memo 组件，并通过稳定事件桥接传递点击、右键、双击和拖拽事件，减少缩略图批量刷新、状态栏更新等场景下的可见项无效重渲染。
  - 原生窗口与 Tauri 事件监听现在会先判断运行环境，浏览器预览不再因缺少 Tauri runtime 出现白屏或控制台错误，方便后续做前端回归验证。
  - 性能审计脚本新增对应检查，防止后续迭代把布局版本隔离和素材项 memo 优化退化掉。
  - 该优化只作用于本机渲染状态，不写入素材文件夹，不改变图片文件、缩略图缓存或文件夹级元数据结构，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`npm run stress:audit`、`cargo check`、本地浏览器预览刷新检查与 `npm run desktop:build` 验证。

## 2026-06-10

- 项目浏览位置与首尾跳转：
  - 将 Picman 版本更新到 `0.1.37`。
  - 中央素材视图的滚动位置现在按资源库、文件夹、标签、视图模式、类型筛选、缩略图状态和排序方式分别记忆；切换到其它项目或目录后再切回来，会回到之前浏览的位置。
  - 新增 `⌘↑` / `⌘↓`：在素材视图中直接跳到当前列表顶部或底部，并同步选中首张或最后一张素材。
  - 该浏览位置属于本机 UI 偏好，继续写入 `localStorage`，不写入素材文件夹，保持文件优先原则。
  - 已通过 `npm run lint`、`npm run build`、`cargo check` 与 `npm run desktop:build` 验证，并安装打开 `/Applications/Picman.app`。

- macOS 关闭方式与本机 UI 状态持久化：
  - 将 Picman 版本更新到 `0.1.36`。
  - macOS 红绿灯关闭按钮现在只隐藏窗口，不退出应用；再次点击 Dock 图标会重新显示并聚焦主窗口。
  - 新增窗口级 `Ctrl+W` 与 `⌘W` 隐藏窗口快捷键；彻底退出仍通过 Dock 图标右键「退出」或系统退出命令完成。
  - 启动时会自动恢复上次打开的本地资源库，避免重新进入默认示例库。
  - 本机 UI 状态新增持久化：当前文件夹、当前标签、视图模式、排序方式、缩略图大小、左侧栏宽度、左侧文件夹/标签分割高度、右侧信息栏显示状态，以及按资源库/文件夹/标签/视图区分的中央滚动位置。
  - 这些偏好写入本机 `localStorage`，不写入图片文件夹，不影响文件优先原则；图片文件、文件夹级元数据和 `.picman` 设置仍是资源库自身的数据源。
  - 已通过 `npm run lint`、`npm run build` 与 `cargo check` 验证。

- 发布瀑布流补丁测试包：
  - 将 Picman 版本更新到 `0.1.35`，用于发布 Claude 修复后的最新版安装包。
  - 当前发布包包含瀑布流宽高比一致性修复：缩略图实际渲染比例与瀑布流虚拟布局预留高度保持一致，避免已生成缩略图后出现错位、重叠或大片空白。
  - 本次只发布当前本地工作区状态，不改变文件优先原则；缩略图仍是可删除、可重建的缓存，素材文件与文件化元数据仍是核心数据源。

## 2026-06-09

- 修复瀑布流视图排版空白和错位：
  - 将 Picman 版本更新到 `0.1.34`。
  - 修复瀑布流视图同时叠加旧 CSS 多列布局与新虚拟化绝对定位的问题，避免列布局和虚拟布局互相干扰导致大片空白。
  - 虚拟素材容器不再额外叠加真实 CSS padding，瀑布流的上、下、左右间距统一由布局算法计算，避免高度和滚动范围被重复计算。
  - 瀑布流素材项写入明确高度，并固定文字区高度，使虚拟化估算高度与实际 DOM 高度保持一致。
  - 修正瀑布流视觉顺序按真实列位置排序，后续方向键移动和可见顺序更稳定。
  - 修复缩略图实际宽高比与布局预留高度不一致导致的错位：素材项内缩略图的 CSS `aspect-ratio` 现在与布局所用的比例来源一致（优先 `width/height`，其次 `thumbnailWidth/thumbnailHeight`，再退回 `dimensions`），避免已生成缩略图（非 16:10）的图片在瀑布流中溢出方框、互相重叠。
  - 已通过 `npm run lint`、`npm run build` 与本地浏览器瀑布流布局检查验证。

- 顶部操作栏精简与回收站按钮修正：
  - 将 Picman 版本更新到 `0.1.33`。
  - 顶部素材操作栏移除常驻的左转、右转、批量调整与压缩、删除按钮，避免单张预览旋转入口与批量选择入口混在一起。
  - 多选素材后，临时出现的「已选择 n 个素材」栏新增批量操作区：左转、右转、调整/压缩、删除到回收站；批量动作只在多选语境出现。
  - 顶部常驻按钮重新分组：排序、筛选、刷新归为素材筛选控制组；自适应、瀑布流、列表归为视图模式组；两组之间使用单条分隔线区分。
  - 回收站视图右上角移除重复的「清空回收站」按钮，只保留「恢复所选」，避免按钮拥挤和文字溢出；清空回收站继续保留在左侧回收站右键菜单中。
  - 已通过 `npm run lint` 与 `npm run build` 验证。

## 2026-05-31

- 批量旋转改用工具栏按钮，避免右键改选区的冲突：
  - 将 Picman 版本更新到 `0.1.32`。之前批量旋转放在素材右键菜单里，但右键有时会把多选collapse成单选，导致只转了一张。
  - 现在在中央工具栏新增两个旋转按钮（向左 / 向右 90°），对当前所选的全部图片旋转，所选数量大于 0 时可用、按钮标题显示数量；不依赖右键、不改变选区。
  - 右键菜单移除旋转项（保留复制图片 / 重命名 / 移动 / 删除）；单张旋转仍可用右侧详情栏的旋转按钮。
  - 已通过 `npm run build`、`npm run lint` 与 Claude Preview 实测（多选 3 张后工具栏旋转按钮可用且标注数量）验证。

- 图片旋转（单张与多选批量，旋转即保存）：
  - 将 Picman 版本更新到 `0.1.31`。右侧详情栏新增两个旋转按钮（向左 / 向右各 90°），点击即旋转原图并保存。
  - 右键素材菜单新增「向右旋转 90°」「向左旋转 90°」，对当前所选的全部图片批量旋转。
  - 新增 Rust `rotate_images`：解码 → 按 90° 步进旋转 → 重新编码写回（JPEG 以质量 95 重编码，PNG/WebP 无损）→ 原子写回 → 重扫，仅处理 PNG/JPG/WebP（SVG/GIF 等按钮自动禁用、批量自动跳过）。
  - 旋转保持原文件名与格式，因此标签/备注/收藏自动保留；因像素内容已改变，旋转后会自动重新生成对应缩略图。
  - 已通过 `cargo test`（17 项，含旋转后宽高互换且元数据保留）、`npm run build`、`npm run lint` 与 Claude Preview 实测（详情栏按钮、右键菜单项）验证。

- 修复快速滑动时已生成缩略图被替换成彩色占位封面的问题：
  - 将 Picman 版本更新到 `0.1.30`。移除「快速滚动时延迟加载缩略图」的降级逻辑——之前快速滑动时新进入视窗的素材即便已有缩略图也会先显示彩色占位封面（标「加载中」），观感不对。
  - 现在只要素材已生成缩略图就直接显示真实缩略图；彩色占位封面只在「待生成」或加载失败时出现。缩略图仍用浏览器懒加载与异步解码，滚动方向上的动态预渲染（overscan）保留，滑动依然顺滑。
  - 同步精简掉不再使用的 `isFastScrolling` 状态与定时器，并更新性能审计不变量。
  - 已通过 `npm run build`、`npm run lint` 与 `npm run stress:audit`（23/23 不变量）验证。

- 打开资源库后无缩略图时弹出生成提示：
  - 将 Picman 版本更新到 `0.1.29`。打开本地资源库扫描完成后，若该库还没有任何缩略图（且素材数大于 0），自动弹出「生成缩略图」提示。
  - 弹窗可全选或按文件夹勾选（默认全选）：全选时为整库（含库根直接放置的图片）生成，部分勾选时只为所选文件夹生成；也可点「稍后」关闭，之后仍可在偏好设置里手动生成。
  - 通过扫描期累计的「已恢复缓存缩略图数量」判断是否需要提示（同步、不受异步渲染影响）；重新打开已生成过缩略图的库不会再提示。
  - 复用既有缩略图生成链路（全部 / 按文件夹），生成仍为后台受控并发、不阻塞浏览。
  - 已通过 `npm run build`、`npm run lint` 验证；该提示依赖原生库打开流程，需在桌面版打开一个尚无缩略图的资源库实测。

- 左侧文件夹右键重命名：
  - 将 Picman 版本更新到 `0.1.28`，左侧文件夹列表支持右键弹出菜单：「重命名」与「在 Finder 中显示」。
  - 新增 Rust `rename_folder`：原子重命名文件夹目录（其中的素材、子文件夹与 `.picman.folder.json` 元数据随目录一起移动），随后递归重扫该文件夹，返回每个受影响素材的「旧相对路径 → 新素材」映射。
  - 前端按映射增量替换受影响素材（含嵌套子文件夹），沿用原缩略图避免重新生成；同时把当前所选文件夹、已记住的文件夹排序中受影响的路径一并改写并持久化，选区也按新旧 id 自动迁移。
  - 复用已有的重命名弹窗组件，默认选中文件夹名；重名或非法名称会报错。
  - 已通过 `cargo test`（16 项，含文件夹重命名重映射含嵌套子目录）、`npm run build`、`npm run lint` 与 Claude Preview 实测（文件夹右键菜单、重命名弹窗）验证。

- 拖出/复制图片与文件管理（移动、重命名、新建文件夹）：
  - 将 Picman 版本更新到 `0.1.27`。
  - 素材现在可直接拖拽到 Figma、Finder、聊天窗口等其它应用（基于 `tauri-plugin-drag` 的原生文件拖拽，支持多选一起拖，带计数角标的拖拽图标）。
  - 右键素材弹出菜单：复制图片到剪贴板（去别处粘贴）、重命名、移动到…、删除到回收站；右键时若该素材不在当前选区会先选中它。
  - 复制图片：新增 Rust `copy_image_to_clipboard`，解码后以 RGBA 写入系统剪贴板（`tauri-plugin-clipboard-manager`）。
  - 移动：右键「移动到…」弹出文件夹选择器，可选已有文件夹或直接「新建文件夹」并移入；多选可一起移动，移动会保留标签/备注/收藏并自动处理重名。
  - 重命名：右键「重命名」弹窗，默认选中文件名主体（不含扩展名），不写扩展名时自动补回原扩展名。
  - 新增 Rust `move_assets` / `rename_asset`：原子移动/重命名文件并搬运文件夹级元数据；前端做增量替换并沿用原缩略图避免重新生成。
  - 已通过 `cargo test`（15 项，含移动+重命名搬运元数据）、`npm run build`、`npm run lint` 与 Claude Preview 实测（右键菜单、重命名弹窗、移动选择器、素材可拖拽）验证；剪贴板复制与原生拖出需在桌面版实机体验。

- 批量调整分辨率与压缩：
  - 将 Picman 版本更新到 `0.1.26`，多选图片后可在顶部工具栏点「批量调整与压缩」（魔棒图标）批量处理原图。
  - 弹窗支持：调整分辨率（不调整 / 限制最长边 px / 缩放百分比）与压缩质量滑块（30–100，仅对 JPEG 有损压缩生效；PNG/WebP 无损，主要靠缩放减小体积）；显示已选与可处理数量、并明确提示会覆盖原图且不可撤销、标签备注收藏会保留。
  - 仅处理 PNG / JPG / WebP（SVG/GIF/AVIF 等自动跳过）；处理为原地覆盖、保持原文件名与格式，因此文件夹级元数据（标签/备注/收藏）自动保留。
  - 新增 Rust `batch_process_images` 命令：解码 → 按需 Lanczos3 缩放 → 按质量重新编码（JPEG 有损、PNG/WebP 无损）→ 原子写回，复用既有图像管线，未引入新依赖；逐张容错，失败计数返回。
  - 处理后走前端增量替换（移除旧素材、插入重扫后的新素材、重新选中并顺序重建其缩略图），并在状态栏显示「体积 X → Y（省 Z%）」。
  - 已通过 `cargo test`（14 项，含缩放重压缩与 WebP 编码能力校验）、`npm run build`、`npm run lint` 与 Claude Preview 实测（工具栏按钮、弹窗、分辨率模式切换、质量滑块）验证。

- 图片删除回收站与多选删除：
  - 将 Picman 版本更新到 `0.1.25`，新增文件优先的回收站：删除图片不会立即销毁，而是移动到资源库根目录下的 `.picman/trash/`，并由 `.picman/trash/trash.json` 清单记录原始路径、删除时间与文件夹级元数据（标签/备注/收藏/采集出处）。
  - 左侧栏底部新增「回收站」入口并显示数量；点击进入回收站视图，可查看、单个或批量「恢复」删除的图片（恢复回原文件夹，遇重名自动加后缀，并还原其元数据）。
  - 右键「回收站」入口弹出菜单「清空回收站」，清空前有确认对话框，确认后永久删除且不可恢复（回收站视图内也有同样的「清空回收站」按钮）。
  - 在网格/列表中多选后，可通过顶部工具栏的删除按钮，或可配置的快捷键删除到回收站；快捷键默认 `⌘⌫`，可在「偏好设置 → 通用 → 删除到回收站快捷键」中点击后按下任意组合键自定义（Esc 取消捕获）。
  - 新增 Rust 命令 `move_to_trash` / `list_trash` / `restore_from_trash` / `empty_trash`，均为原子文件操作并复用既有扫描/元数据管线；删除与恢复在前端走增量更新，不触发全库重扫。
  - 已通过 `cargo test`（12 项，含移动→列出→恢复→清空回收站全流程）、`npm run build`、`npm run lint` 与 Claude Preview 实测（回收站入口与视图、空状态、工具栏删除按钮、快捷键捕获）验证。

- 图片文字识别（OCR）：
  - 将 Picman 版本更新到 `0.1.24`，在右侧详情栏新增「识别文字」按钮，点击即可识别当前图片中的文字。
  - 识别结果弹窗：文字可直接在文本框中编辑，并提供「复制」按钮一键复制；识别文本会去除所有换行与空行、合并为一段连续文字。
  - OCR 服务商接入 OCR.space：在「偏好设置 → 文字识别（OCR）」中新增 API Key 输入框与识别语言选择（简体/繁体中文、英文、日文、韩文），配置持久化到本地。
  - 新增 Rust `prepare_image_for_ocr` 命令：读取源图、按最长边 2048 限制缩放并编码为 JPEG 后做 base64（内置轻量 base64 编码，未引入新依赖），既复用既有图像管线又满足 OCR.space 的体积与格式要求；矢量图（SVG）暂不支持，按钮自动禁用。
  - OCR 请求由前端直接调用 OCR.space（其 API 支持跨域），结果按 `\s+→空格` 合并为单段。
  - 已通过 Claude Preview 实测全链路：详情栏按钮、未配置 Key 的提示弹窗、OCR.space 真实请求（多行文本被正确合并为一段）、可编辑文本框与复制按钮、偏好设置配置项；并通过 `cargo test`（11 项，含 base64 向量与 OCR 预处理）、`npm run build`、`npm run lint` 验证。

- 偏好设置面板按主次重排与控件多样化：
  - 将 Picman 版本更新到 `0.1.23`。
  - 按重要性重排分区：把「缩略图缓存」提到资源库之后作为主区，「外观 / 方向键 / 软件更新」等次要项统一下移到缩略图设置之后。
  - 控件不再清一色用分段药丸：把「外观」和「方向键」合并进一个紧凑的「通用」分区——主题模式改用带图标的分段选择器（系统/浅色/深色，参考 macOS 外观选择器），方向键切换改用下拉菜单（`select`，带自定义箭头并适配深浅色），缓存上限仍用滑块、生成品质仍用分段，整体呈现层次更分明、形式更成熟。
  - 已通过 Claude Preview 实测（分区顺序、图标分段、下拉控件在深浅两色下均正常）与 `npm run build`、`npm run lint` 验证。

- 主题手动切换与浅色顶栏分隔线：
  - 将 Picman 版本更新到 `0.1.22`。
  - 偏好设置新增「外观 · 主题模式」分段控件，可在「跟随系统 / 浅色 / 深色」之间手动切换；选择后即时生效，无需重启。
  - 主题改为由根节点 `data-theme` 属性驱动：`index.html` 内联脚本在首屏绘制前根据偏好解析主题，避免闪烁；偏好持久化到 `localStorage`，「跟随系统」模式下仅注册一个事件驱动的 `matchMedia` 监听器实时跟随系统外观变化，不引入轮询或常驻开销。
  - 浅色模式下为顶栏补充一道很浅的描边分隔线（`rgba(0,0,0,0.09)`），让顶栏与下方内容自然区分，柔和不生硬。
  - 浅色模式下分段控件选中态沿用原生「白色悬浮药丸」样式。
  - 已通过 Claude Preview 实测三种模式（跟随系统解析为浅色、强制浅色、在浅色系统下强制深色均正确）与 `npm run build`、`npm run lint`、`npm run stress:audit`（23/23 不变量）验证；深色模式经实测确认未因浅色改造而回退。

- 浅色模式（跟随系统外观，零运行时开销）：
  - 将 Picman 版本更新到 `0.1.21`，新增浅色外观支持，跟随 macOS「系统设置 → 外观」在浅色/深色间自动切换。
  - 实现完全基于 CSS `@media (prefers-color-scheme: light)` 媒体查询，没有任何 JS、监听器或主题状态，切换在系统层完成，符合「不增加资源消耗」的原则。
  - 移除 `tauri.conf.json` 中强制的 `"theme": "Dark"`，窗口外观改为跟随系统；并在 `index.css` 为根调色板新增浅色覆盖与 `color-scheme: light dark`，让滚动条等原生控件也自动适配。
  - 将 `App.css` 中分散的硬编码灰阶（背景、描边、文字、悬浮叠加层）归并到语义化 CSS 变量；高频与关键颜色映射为其精确等值变量，深色模式保持原样，仅极少数一次性灰阶在同色系内有不可察觉的微调。
  - 选中态（分段控件等）在深色下是高对比浅色药丸，浅色下补充原生「白色悬浮药丸」样式，避免反相成突兀的深色块。
  - 已通过浅色实测（侧栏、素材墙、标签、偏好设置面板、选中态、文字对比度均正常）与 `npm run build`、`npm run lint`、`npm run stress:audit`（23/23 不变量）验证；浅色配色经页面级扫描确认无不可见文字或残留深色块。

- 左侧栏资源库入口与文件夹手动排序：
  - 将 Picman 版本更新到 `0.1.20`。
  - 移除左侧栏资源库标题右侧无实际用途的下拉三角，替换为「在 Finder 中显示」的文件夹图标按钮；点击通过 Rust `reveal_in_finder` 用 `open` 在系统 Finder 中定位到当前资源库根文件夹（未打开本地资源目录时按钮禁用）。
  - 左侧文件夹列表支持手动拖拽排序：按住文件夹上下拖动即可调整顺序，拖动时显示插入位置高亮；搜索过滤或仅有一个子文件夹时自动禁用拖拽，避免对过滤后的子集排序产生歧义。
  - 文件夹顺序遵循文件优先理念，持久化到资源库根目录的 `.picman/settings.json`（新增 `folderOrder` 字段，可读、可同步、可删除重建）；打开资源库时读取该顺序，未排序过的新文件夹按字母序追加在已排序文件夹之后。
  - 新增 Rust `read_library_settings` / `write_library_settings` 命令，原子写入 `.picman/settings.json`，与文件夹级 `.picman.folder.json`、缓存目录 `.picman/cache` 一起构成完整的文件优先存储结构。
  - 文件夹拖拽排序的数据载荷使用自定义类型，不会触发图片采集的全局拖放收集逻辑，两者互不干扰。
  - 已通过 `npm run build`、`npm run lint`、`cargo test`（9 项）与 `npm run stress:audit`（23/23 不变量）验证。

- 图片采集（粘贴 / 拖入即收藏到文件夹）：
  - 将 Picman 版本更新到 `0.1.19`，新增「从浏览器或其它应用把图片收藏到不同文件夹」的纯应用内采集能力，不引入托盘、浏览器扩展或常驻服务。
  - 复制图片后 `Cmd+V` 粘贴、系统截图后粘贴、或把图片拖入窗口即可触发采集；本地/应用内图片直接读取字节，浏览器跨域 URL 做尽力下载（失败时提示改用“复制图片”再粘贴）。
  - 采集时弹出轻量「收藏到…」浮层：默认落到库根下的 `Inbox`，记住上次选择，并可即时筛选、用上下键和回车快速选其它文件夹。
  - 新增 Rust `collect_image` 命令统一落库：按真实格式嗅探纠正扩展名，bmp/tiff/ico 等不可直接显示的格式转码为 PNG，其余格式保留原始字节；按内容 SHA-256 在目标文件夹内去重，重复图片不再重复保存。
  - 采集结果是目标文件夹里的普通图片文件，来源 URL 与采集时间写入该文件夹的 `.picman.folder.json`（新增 `sourceUrl`、`capturedAt` 字段，向后兼容）；编辑标签/收藏/备注时会保留这些采集出处。
  - 采集后走增量插入而非全库重扫，自动跳转到目标文件夹、选中新图并单独生成其缩略图，不打断正在进行的批量缩略图任务。
  - 窗口改为 `dragDropEnabled:false`，由 webview 接管拖放以便接住浏览器拖来的图片 URL；应用本就不依赖系统级文件路径拖放。
  - 已通过 `npm run build`、`npm run lint`、`cargo test`（9 项，含采集落库/去重/转码/出处保留）与 `npm run stress:audit`（23/23 不变量）验证。

- 大素材库状态结构精简与交互性能优化（无新增运行时资源消耗）：
  - 将 Picman 版本更新到 `0.1.18`，用于打包验证状态结构精简后的扫描与交互性能。
  - 合并重复的素材数组：删除与 `libraryAssets` 内容始终一致的 `libraryCatalogAssets` 状态，文件夹、标签、体积统计与筛选改为同一份源数组派生，扫描期不再对两个 2.5 万级数组重复浅拷贝。
  - 删除冗余的 `catalogAssetById` 索引：素材墙布局直接复用原地变更、引用稳定的 `assetStore.byId`，扫描每个批次不再从零重建一张 2.5 万条 Map。
  - 收藏切换改走增量路径：点击收藏只更新增量 `assetStore` 并写回 `.picman.folder.json`，不再复制主素材数组，也不再连锁触发目录派生、可见列表重算与多张索引 Map 重建；大库中单次收藏从约 6 次全量遍历降为一次 O(1) 更新。
  - 去重可见项索引：右侧视图层复用 App 计算的可见项 `id→index` 索引，移除 `LibraryView` 内重复的同名 `useMemo`；Shift 连续选择改用该索引 O(1) 定位，替代两次 `indexOf` 全量扫描。
  - Rust 扫描缓存探测按需进行：每次扫描先一次性探测 `.picman/cache/thumbnails` 下实际存在的质量目录，仅对存在缓存的质量做哈希与文件命中检查；首次打开无缓存的素材库时不再为每个文件做必然落空的哈希与多次 stat 系统调用，加快初次扫描。
  - 收藏状态与缩略图一样与源数组解耦，刷新合并与重新扫描会从实时索引和文件夹元数据正确恢复，行为保持一致。
  - 已通过 `npm run build`、`npm run lint`、`cargo test`（6 项）与 `npm run stress:audit -- --library=/tmp/picman-stress-25000 --min-count=25000`（23/23 不变量）验证。

- 文件夹级元数据文件落地：
  - 将 Picman 版本更新到 `0.1.17`，用于测试文件优先的标签、备注和收藏持久化。
  - 每个图片文件夹默认使用一个 `.picman.folder.json` 保存该文件夹内素材的标签、备注和收藏状态，不再为每张图片默认创建旁车 JSON。
  - 打开资源目录时，Rust 后台扫描会按文件夹缓存读取 `.picman.folder.json`，并把元数据恢复到素材状态中，避免逐文件重复读取。
  - 在右侧信息栏添加、移除标签，切换收藏或编辑备注后，会原子写回当前素材所在文件夹的 `.picman.folder.json`。
  - `.picman.folder.json` 是用户可读、可复制、可同步的源数据；`.picman/cache` 仍然只保存可删除、可重建的性能缓存。
  - 架构文档更新为当前文件格式：根目录 `.picman/settings.json`、文件夹内 `.picman.folder.json`、缓存目录 `.picman/cache`。

- 文件优先前提下的缩略图复用与高速滚动优化：
  - 将 Picman 版本更新到 `0.1.16`，用于测试重复打开大素材库和快速滑动时的体验优化。
  - 打开资源目录时会检查 `.picman/cache/thumbnails` 中已有且仍匹配源文件、算法版本和质量档位的缩略图，并恢复到素材状态中；缓存仍然只是可删除、可重建的文件化性能产物。
  - 生成缩略图前会先检查同质量缓存是否已经存在，未变化文件直接复用缓存，不再重复解码和重写图片。
  - 25000 张素材库再次打开后可以直接显示已生成的缩略图状态，设置面板统计也会随着扫描批次增量恢复。
  - 中央素材墙新增快速滚动模式：保留 macOS 原生滚动手感，根据滚动速度动态扩大滚动方向上的预渲染范围。
  - 快速滑动时新进入视窗的缩略图会先显示轻量占位，滚动稳定后再加载真实图片，避免图片解码跟不上时拖慢滚动。
  - 虚拟素材项定位改为 `translate3d`，并为缩略图图片启用浏览器异步解码和懒加载，降低滚动时主线程和布局压力。

## 2026-05-29

- 25000 素材库缩略图显示与缓存体积优化：
  - 将 Picman 版本更新到 `0.1.15`，用于发布 25000 素材库实测修复包。
  - 修复生成后的缩略图在应用内显示问号或“加载失败”的问题：本地资源目录授权现在会显式放行隐藏的 `.picman` 缓存目录，避免 macOS/Tauri 资产协议对点目录的匹配限制导致缓存文件不可读。
  - 缩略图编码改为透明度感知策略：只有真实透明图片继续输出 PNG，普通不透明 RGBA 图片改用 JPEG，避免把大量可压缩素材保存成体积过大的 PNG。
  - 优化缩略图尺寸与质量档位，标准档最大边控制在 240px，并收紧小文件直接复用阈值；小图不会再被无意义放大，小但体积重的图片会重新编码压缩。
  - 新增 Rust 单元测试覆盖不透明 RGBA 转 JPEG、透明 PNG 保留 PNG、小体积重 PNG 重编码且不放大三类关键路径。
  - 性能审计新增 `.picman` 缓存目录授权和透明度感知编码检查，`npm run stress:audit -- --library=/tmp/picman-stress-25000 --min-count=25000` 已从 15 条扩展到 17 条不变量并全部通过。
  - 已用打包后的桌面版打开 `/tmp/picman-stress-25000`，完成 25000 张素材的标准档缩略图生成；生成过程中界面可继续操作，并已验证生成中切换列表视图正常。
  - 25000 张标准档缩略图最终生成 25000 个缓存文件，占用约 `159M`，其中 `21250` 个 JPG、`2500` 个 PNG、`1250` 个 SVG；相较旧算法约 `779M` 的缓存体积明显下降。

- 增加 25000 素材库性能审计入口：
  - 将 Picman 版本更新到 `0.1.14`，用于固定 25000 素材库性能优化后的回归审计链路。
  - 新增 `npm run stress:audit`，用于检查大库性能关键不变量：本地扫描必须后台流式、小批次事件必须存在、素材墙必须虚拟化、视图层不能退回全量 `visibleAssets`、缩略图状态必须与源素材数组解耦、缩略图批次不能更新主素材数组、缩略图后台并发必须受控。
  - 性能审计脚本支持传入压力素材库路径和最小素材数量，会递归统计图片文件，确认 `/tmp/picman-stress-25000` 这类测试库确实达到 25000 张素材规模。
  - 新增 `docs/PERFORMANCE.md`，记录 25000 素材库目标场景、打开扫描、虚拟渲染、筛选排序、缩略图后台生成与完成证据要求。
  - README 增加 `stress:audit` 命令说明，后续每次性能相关改动都能用同一套审计入口防止回退。
  - 已完成 `npm run stress:audit -- --library=/tmp/picman-stress-25000 --min-count=25000`，15 条性能不变量全部通过。
  - 已完成 `npm run lint`、`npm run build`、`cargo check`、`npm run desktop:build`、`npm run release:prepare` 与签名校验验证。
  - 已发布 GitHub Release `v0.1.14`，公开更新清单已指向 `0.1.14`。

- 缩略图后台生成受控并发：
  - 将 Picman 版本更新到 `0.1.13`，用于测试 25000 张素材缩略图生成时的后台吞吐与系统占用平衡。
  - Rust 原生缩略图生成从单线程顺序处理改为受控 worker 池，最多使用 3 个后台 worker，并按机器可用并行度保守取值，避免长时间生成时占满 CPU。
  - 后台 worker 只负责解码、缩放、编码和落盘，统一由聚合器收集结果并按批次发送给前端，继续避免高频事件风暴。
  - 取消缩略图任务时，worker 会停止继续领取新素材，只完成已在处理中的少量任务；完成事件现在携带真实已完成数量，取消状态不再显示成全部完成。
  - 批次进度保留当前处理文件名，前端仍沿用节流写回和增量索引，保证生成期间素材墙可继续滚动、筛选和切换视图。
  - 已完成 `cargo check`、`npm run lint`、`npm run build`、200 张 smoke 压力库生成、25000 张压力库生成、`npm run desktop:build`、`npm run release:prepare` 与签名校验验证。
  - 已发布 GitHub Release `v0.1.13`，公开更新清单已指向 `0.1.13`。

- 缩略图状态与源素材数组解耦：
  - 将 Picman 版本更新到 `0.1.12`，用于测试 25000 张素材生成缩略图时的主线程数组复制优化。
  - 缩略图生成、清理缓存和浏览器回退缩略图写回不再高频更新 `libraryAssets` 主数组，避免每批结果都浅拷贝 25000 条素材。
  - 缩略图状态改为集中写入增量 `assetStore`，素材墙、右侧详情和灯箱都从该索引读取实时缩略图状态。
  - 默认“全部”缩略图状态下，缩略图批次进度不会触发全量可见列表重新过滤；只有切到“已生成/未生成”筛选时才按当前索引实时计算缩略图状态。
  - 重新生成、按文件夹生成、清理缓存和刷新资源库都会按需从实时索引合成当前素材列表，保证缓存统计、旧缩略图释放和刷新保留缩略图状态仍然正确。
  - 已完成 `npm run lint`、`npm run build`、`cargo check`、200 张 smoke 压力库生成、`npm run desktop:build`、`npm run release:prepare` 与签名校验验证。
  - 已发布 GitHub Release `v0.1.12`，公开更新清单已指向 `0.1.12`。

- 缩略图批次写回主线程减负：
  - 将 Picman 版本更新到 `0.1.11`，用于测试 25000 张素材生成缩略图时的进一步流畅度。
  - 素材墙的数据通道从“传递全量可见素材对象数组”改为“传递可见素材 ID 顺序 + 按需查表”，虚拟视图只为屏幕附近的卡片读取素材对象。
  - 默认自适应视图和列表视图在缩略图批次更新时不再重新构造 25000 个 `visibleAssets` 对象，滚动布局也不再被缩略图进度刷新触发全量派生。
  - 瀑布流布局拆成稳定的布局 ID 与实时素材查表，默认全部素材视图下缩略图更新不会反复重算全量瀑布位置。
  - `assetById` 从每批缩略图结果都由 25000 条素材重建，改为可控的增量索引 store；每批只更新本批素材 ID，并用轻量版本号触发界面刷新。
  - 素材数组写回从全量 map 改为基于全局 ID 索引的数组浅拷贝与定点替换，避免每批缩略图都逐条判断 25000 个素材。
  - 标签增删、打开资源目录、浏览器回退扫描、刷新资源库、清理缩略图缓存和缩略图生成都已同步维护增量索引，避免 UI 数据源不一致。
  - 已完成 `npm run lint`、`npm run build`、`cargo check`、200 张 smoke 压力库生成、25000 张压力库生成、`npm run desktop:build`、`npm run release:prepare` 与签名校验验证。
  - 已用桌面版打开 `/tmp/picman-stress-25000`，确认 25000 素材库可显示、搜索清空后可恢复全量素材，并可切换自适应、列表和瀑布流视图。
  - 已发布 GitHub Release `v0.1.11`，公开更新清单已指向 `0.1.11`。

- 缩略图统计增量化：
  - 将 Picman 版本更新到 `0.1.10`，用于测试 25000 张素材缩略图生成期间的统计面板低开销更新。
  - 缩略图统计从每次素材状态变化后全量遍历资源库，改为维护 `已生成数量` 与 `缓存占用` 两个增量计数器。
  - 打开新资源目录、浏览器回退扫描、刷新资源库、清理缩略图缓存和重新生成缩略图时，统计会在对应节点重置、重算或按批次增量更新。
  - 后台缩略图批次写回时，只按本批新增成功的缩略图数量和体积更新统计，不再对 25000 条素材做完整 reduce。
  - 修复发布脚本的版本排序：`v0.1.10` 之后的测试包不再因为字典序被排到 `v0.1.9` 前面，GitHub Release 上传会按语义版本选择最新目录。
  - 已完成 `npm run lint`、`npm run build`、`cargo check`、200 张 smoke 压力库生成、`npm run desktop:build`、`npm run release:prepare` 与签名校验验证；发布产物已正确生成到 `release/github/v0.1.10`。
  - 已发布 GitHub Release `v0.1.10`，公开更新清单已指向 `0.1.10`。

- 缩略图生成期间可见顺序优化：
  - 将 Picman 版本更新到 `0.1.9`，用于测试 25000 张素材生成缩略图期间的交互流畅度。
  - 将稳定的资源目录数据与高频变化的缩略图状态拆分：目录、标签、文件夹、搜索文本和排序依据保存在独立 catalog 数据中，缩略图批次只更新素材展示状态。
  - 默认“全部缩略图状态”视图下，后台缩略图批次写回不再触发 25000 条素材的重新筛选和重新排序；只有搜索、文件夹、标签、类型、排序方式或缩略图状态过滤变化时才重建可见顺序。
  - `已生成/未生成` 缩略图过滤仍使用实时素材状态，保证用户主动查看缩略图生成进度时列表内容准确变化。
  - 标签添加和移除会同步更新 catalog 数据，避免搜索和标签筛选使用过期字段。
  - 已完成 `npm run lint`、`npm run build`、`cargo check`、200 张 smoke 压力库生成、`npm run desktop:build` 与 `npm run release:prepare` 验证。
  - 已在桌面版 25000 素材压力库状态下确认搜索 `asset-24999`、缩略图统计和设置面板基础显示仍正常。
  - 已发布 GitHub Release `v0.1.9`，公开更新清单已指向 `0.1.9`。

- 大素材库打开阶段主线程减负：
  - 将 Picman 版本更新到 `0.1.8`，用于测试 25000 张素材打开阶段的主线程减负。
  - 资源库派生信息改为单次遍历生成：标签列表、左侧文件夹、缩略图设置面板文件夹、源文件总量、已生成缩略图数量和缓存体积不再分别完整扫描素材数组。
  - 打开本地资源目录时新增扫描状态标记；扫描未结束且可见素材超过 2000 个时，中央区域先按后台扫描顺序增量展示，避免每批素材进入前端后都对上万条记录重新排序。
  - 扫描完成后自动恢复当前排序规则，保证最终素材列表仍按用户选择的排序方式显示。
  - 素材排序改用复用的 `Intl.Collator`，名称排序支持数字自然顺序，并为日期、类型和同名素材增加稳定兜底排序。
  - 已完成 `npm run lint`、`npm run build`、`cargo check`、200 张 smoke 压力库生成、`npm run desktop:build` 与 `npm run release:prepare` 验证。
  - 已用桌面版在 `/tmp/picman-stress-25000` 上完成 25000 素材刷新冒烟，刷新后状态栏显示 `已刷新 · 25000 个素材`，搜索 `asset-24999` 与自适应视图切换正常。
  - 已发布 GitHub Release `v0.1.8`，公开更新清单已指向 `0.1.8`。

- 大素材库性能目标与压力测试基准：
  - 将 Picman 版本更新到 `0.1.7`，用于测试大素材库压力基准和浏览器回退扫描优化。
  - 创建持续性能优化目标：围绕 25000 张图片、多级子文件夹、本地打开不阻塞、扫描增量化、渲染虚拟化、筛选和选择流畅、缩略图后台生成展开后续优化。
  - 新增 `npm run stress:create` 压力素材库生成命令，默认可生成 25000 个素材与 120 个多级子文件夹，PNG 与 SVG 混合，便于后续每次优化做同一口径验证。
  - 新增 `scripts/create-stress-library.mjs`，不依赖额外 npm 包，直接生成测试图片、目录结构和 `.picman-library.json` 元信息。
  - 已生成完整压力库 `/tmp/picman-stress-25000`，实测生成 25000 个素材、120 个文件夹，用时 1117ms。
  - 已用桌面版 Picman 打开 `/tmp/picman-stress-25000`，状态栏显示 25000 个素材，左侧目录树、中央素材墙和右侧详情面板均可正常显示。
  - 已验证中央搜索 `asset-24999` 可快速筛选到 1 个素材，并可切换到列表视图与选中查看。
  - 浏览器文件夹回退扫描改为分批扫描：不再对全部图片一次性读取尺寸和解码，避免 25000 张图片通过浏览器文件输入时 `Promise.all` 解码导致界面卡死。
  - 浏览器回退路径现在按 500 个文件一批写入资源库，并在每批之间让出一帧，保证开发预览或原生打开失败时仍能看到增量进度。
  - README 增加压力库生成命令，后续性能回归可直接复用。
  - 已完成 `npm run lint`、`npm run build`、`cargo check`、`npm run desktop:build`、`npm run release:prepare` 与 200 张 smoke 压力库生成验证。
  - 已发布 GitHub Release `v0.1.7`，公开更新清单已指向 `0.1.7`。

- 缩略图结果写回节流优化：
  - 将 Picman 版本更新到 `0.1.6`，用于测试缩略图生成期间的前端低占用写回。
  - Rust 缩略图事件批次从 64 个提升到 128 个，减少 25000 张图片生成时的事件数量。
  - 前端新增缩略图结果内存队列，收到后台批次后先合并到当前任务，不再每个批次都立刻全量更新素材数组。
  - 前端按 180ms 或累计 256 个缩略图结果统一 flush，一次性批量写回 React 状态，降低缩略图生成期间的主线程重渲染压力。
  - 缩略图进度和状态栏跟随 flush 节奏更新，保持可观察进度，同时减少无意义的高频 UI 刷新。
  - 任务完成、取消或组件卸载时会清理待 flush 定时器，避免旧任务延迟写回。
  - 已完成 `cargo check`、`npm run lint`、`npm run build`、本地浏览器预览和 `npm run desktop:build` 验证。
  - 已发布 GitHub Release `v0.1.6`，公开更新清单已指向 `0.1.6`。

- 大素材库缩略图生成优化：
  - 将 Picman 版本更新到 `0.1.5`，用于测试大素材库缩略图后台批处理链路。
  - 新增 Rust 后台缩略图批处理命令 `generate_thumbnails_stream`，本地资源库生成缩略图时一次提交任务，不再由前端对每张图片逐次调用原生命令。
  - Rust 后台线程会连续处理目标素材，并按 64 个缩略图一批发送 `picman-thumbnail-batch` 事件，前端批量写回缩略图路径、尺寸、大小和失败信息。
  - 新增 `picman-thumbnail-finished` 事件，生成完成后统一更新完成状态、失败数量和状态栏提示。
  - 新增 `cancel_thumbnail_generation` 取消入口；打开新资源目录、刷新资源库或清理缩略图缓存时会取消旧任务，避免旧任务继续占用 CPU 或写回过期结果。
  - 清理缩略图缓存时会先取消后台缩略图任务，再删除 `.picman/cache/thumbnails`，避免边生成边删除造成状态错乱。
  - 浏览器开发环境仍保留 Canvas 缩略图回退；Tauri 本地资源库默认使用 Rust 后台批处理。
  - 已完成 `cargo check`、`npm run lint`、`npm run build`、`npm run desktop:build` 和签名校验验证。
  - 已发布 GitHub Release `v0.1.5`，公开更新清单已指向 `0.1.5`。

- 大素材库性能优化第三轮：
  - 将 Picman 版本更新到 `0.1.4`，用于测试虚拟滚动与后台刷新的进一步优化。
  - 中央素材区虚拟布局拆分为“尺寸变化时计算布局、滚动时只截取可见项”，自适应视图和列表视图滚动时不再为全部素材重建位置表。
  - 瀑布流视图改为缓存全量瀑布布局和视觉顺序，滚动时通过已排序的位置列表截取视窗附近素材，减少 25000 张图片场景下的滚动计算量。
  - 键盘选择滚动改为按当前视图的虚拟位置即时计算目标位置，避免依赖每次滚动生成的全量 `positionById`。
  - 打开资源目录时前端继续分批显示，但同一时间窗口内的扫描批次会合并写入 React 状态，降低快速扫描时的状态更新次数。
  - 刷新资源库按钮改用 Rust 后台流式扫描，不再调用同步全量扫描；刷新期间保留当前素材墙可操作，扫描完成后一次性合并新增、移除和已有缩略图状态。
  - 中央素材搜索结果使用延迟查询值参与过滤，中文或大库搜索输入时优先保证输入框响应。
  - 已完成 `npm run lint`、`npm run build`、`npm run desktop:build` 和本地浏览器预览验证，三种视图切换无前端错误。
  - 已发布 GitHub Release `v0.1.4`，公开更新清单已指向 `0.1.4`。

- 大素材库性能优化第二轮：
  - 将 Picman 版本更新到 `0.1.3`，用于测试后台分批扫描链路。
  - 新增 Rust 后台扫描命令 `scan_library_folder_stream`，打开本地资源目录时立即返回，由后台线程继续遍历文件。
  - 扫描结果按 500 个素材一批通过 Tauri 事件发送给前端，前端边接收边追加显示，不再等待 25000 张图片全部扫描完成后才更新界面。
  - 前端为每次扫描生成独立 `scanId`，新的扫描会自动忽略旧扫描线程的后续事件，避免快速切换文件夹时写入过期结果。
  - 本地扫描不再为每个素材预先生成 `convertFileSrc` 预览地址，改为只保存真实文件路径，大图预览时再懒加载转换，减少大库打开阶段的字符串处理和 JSON 体积。
  - 扫描过程中状态栏实时显示已发现素材数量，扫描完成后显示最终数量。
  - 已完成 `npm run build`、`npm run lint`、`npm run desktop:build` 与 `npm run release:prepare` 验证。
  - 已发布 GitHub Release `v0.1.3`，公开更新清单已指向 `0.1.3`。

- 大素材库性能优化第一轮：
  - 将 Picman 版本更新到 `0.1.2`，用于发布 25000 张图片场景的性能测试包。
  - 中央素材区改为虚拟渲染，自适应视图、瀑布流视图和列表视图都只挂载视窗附近的素材卡片，避免一次性渲染上万 DOM 节点。
  - 虚拟布局保留键盘选择滚动逻辑，选中项超出视窗时按虚拟位置自然滚动到可见区域。
  - 瀑布流视图改为基于素材比例估算列布局，并生成稳定的视觉顺序供方向键使用。
  - 素材搜索文本在扫描后预先归一化缓存，输入搜索时不再对每个素材反复拼接和规格化文本。
  - 素材统计、主选中素材和可见索引改为 memo 化，选择变化不再反复全量 reduce 或 find。
  - Rust 扫描资源目录时不再默认逐张读取图片尺寸，优先快速列出文件、大小、路径和修改时间，降低打开 25000 张图片目录时的首轮阻塞。
  - 缩略图生成改为批量写回素材状态，进度显示做节流，避免每生成一张缩略图就对整个素材数组做一次全量更新。
  - 已完成 `npm run build`、`npm run lint`、`npm run desktop:build` 与 `npm run release:prepare` 验证。
  - 已发布 GitHub Release `v0.1.2`，公开更新清单已指向 `0.1.2`。

- 修复测试版 macOS 无法出现“仍要打开/允许”入口：
  - 将 Picman 版本更新到 `0.1.1`，准备发布新的测试包。
  - macOS 打包改为对完整 `.app` bundle 做 ad-hoc 签名，而不是只依赖主二进制的 linker ad-hoc 签名。
  - 关闭未公证场景下无必要的 hardened runtime，避免测试包被 Gatekeeper 判成签名状态异常。
  - 目标是让系统将 Picman 识别为普通“未识别开发者”应用，用户可通过右键打开或隐私与安全中的“仍要打开”确认启动。
  - 已验证 `codesign --verify --deep --strict` 通过，`spctl` 结果变为普通未认证拒绝，不再出现签名资源异常。
  - 已发布 GitHub Release `v0.1.1`，提供新的 Apple Silicon DMG 和 Tauri updater 产物。

- 接入 Tauri Updater 与 GitHub Releases 更新链路：
  - 安装 `@tauri-apps/plugin-updater`、`@tauri-apps/plugin-process`、`tauri-plugin-updater` 与 `tauri-plugin-process`。
  - 在 Tauri 配置中开启 `createUpdaterArtifacts`，构建时生成 `.app.tar.gz` 更新包及 `.sig` 签名文件。
  - 生成 Picman 专用更新签名密钥，私钥保存在本机 `~/.tauri/picman-updater.key`，构建脚本通过 `TAURI_SIGNING_PRIVATE_KEY` 与空密码环境变量完成无交互签名，只将公钥写入应用配置。
  - 设置 GitHub Releases 静态更新清单地址，默认指向 `zacharycode/picman`，客户端通过 `latest.json` 判断和下载更新。
  - 设置面板新增“软件更新”区域，支持检查 GitHub 更新、显示下载进度、安装后重启应用。
  - 新增 GitHub Release 产物整理脚本，自动生成 `latest.json` 并复制更新包、签名和 DMG 到统一发布目录。
  - 新增可选 GitHub CLI 发布脚本，后续确认真实仓库后可一条命令上传 Release。
  - 已完成 `npm run lint`、`npm run build`、`npm run desktop:build` 与 `npm run release:prepare` 验证，发布产物已生成到 `release/github/v0.1.0/`。
  - 已创建 GitHub 私有仓库 `zacharycode/picman`，用于后续源码托管与私有测试版发布。
  - `.gitignore` 增加密钥和环境变量文件规则，避免 updater 私钥或本地凭据被误提交。
  - 初始化本地 git 仓库并推送当前 Picman MVP 源码到 `main` 分支。
  - 已创建 GitHub Release `v0.1.0`，上传 DMG、Tauri updater 压缩包、签名文件和 `latest.json`。
  - 为简化跨设备测试链路，将 GitHub 仓库 `zacharycode/picman` 调整为公开仓库，使 Tauri Updater 能直接匿名访问 Release 更新清单和更新包。
  - 已验证公开更新清单地址返回 `200`，当前 updater 静态 JSON 链路可直接使用。

## 2026-05-28

- 修正修改时间显示：
  - Rust 扫描资源目录时改用 `chrono` 输出 `YYYY-MM-DD` 格式的修改日期。
  - 修复 Tauri 路径下右侧素材信息和列表视图显示为 `<秒数> 秒` 的问题。
  - 与示例素材和浏览器导入的修改日期格式保持一致。

- 实现真正的"刷新资源库"：
  - 顶部刷新按钮改为重新扫描磁盘，而不是只刷新状态栏文字。
  - 重扫后保留已生成缩略图的素材状态，避免再次手动生成。
  - 重扫后保留尚存于资源库的当前选中素材，被删除的素材自动从选中态移除。
  - 状态栏会汇总刷新结果，新增或移除素材时显示数量。
  - 浏览器环境下没有持久化路径，刷新会提示需先打开本地资源目录。

- 移除"压缩缩略图缓存"假按钮：
  - 该按钮原先只是把缓存占用显示数字按固定比例缩小，并未真正写盘压缩。
  - 删除偏好设置中的压缩按钮和对应的 `cacheCompression` 状态。
  - 缓存占用现在直接反映已生成缩略图的实际大小总和。
  - 真正的压缩能力等接入 mozjpeg / oxipng 等工具后再加回。

## 2026-05-26

- 优化左侧栏上下分割线：
  - 标签为空时仍保留“文件夹 / 标签”两个面板和中间水平分割线。
  - 中间水平分割线在无标签状态下也可以上下拖动。
  - 文件夹区缩小时，文件夹列表改为内部滚动，列表项不再被压缩变形。
  - 标签区缩小时同样保持列表行高稳定，通过滚动浏览内容。

- 精简顶部栏：
  - 移除顶部栏左侧“本地资源库”文字。
  - 移除文字区域遗留的左侧顶栏分割线，不再保留空白分栏痕迹。
  - 保留顶部栏空白区域作为窗口拖拽热区，使界面更接近桌面工具的沉浸式顶栏。

- 支持中文搜索输入：
  - 中央素材搜索框增加中文输入法组合输入处理，拼音组词期间不再打断候选词提交。
  - 左侧文件夹搜索框增加同样的组合输入处理，中文文件夹名可以正常输入和筛选。
  - 搜索匹配统一做 Unicode 规格化和大小写归一，兼容中文、英文以及全角字符混用。

- 修复本地图片预览和缩略图显示为问号：
  - 开启 Tauri asset protocol，使通过原生文件夹选择器授权后的本地图片路径可以被前端图片组件正常读取。
  - 选择资源目录时改为递归授权，子文件夹内的图片和 `.picman/cache` 内生成的缩略图都能被预览组件读取。
  - Rust 生成缩略图后继续写入 `.picman/cache`，同时返回轻量 data URL 给前端显示，避免隐藏缓存目录在 WebView 中触发破图。
  - 小于目标缩略图尺寸的 PNG、JPG、WebP 不再重新编码，直接复制到缓存，避免小图被重新编码后体积变大。
  - PNG 缩略图改用更高压缩等级，降低透明图缓存体积。
  - 保持资源目录授权为动态作用域，不把图片文件复制进应用内部，继续遵守“文件本身是第一性”的设计原则。
  - 大图预览增加缩略图兜底：如果原图预览地址加载失败，会优先回退到已生成缩略图，避免出现系统破图问号。

- 接入 Rust 原生缩略图缓存管线：
  - 资源目录选择改为优先使用 Tauri 原生文件夹选择器，获得真实本地路径授权。
  - 新增 Rust 命令扫描资源目录，递归读取图片文件并跳过 `.picman` 缓存目录。
  - 新增 Rust 命令生成缩略图，缩略图写入资源库 `.picman/cache/thumbnails/<quality>/`。
  - 缩略图缓存文件名按算法版本、文件路径、文件大小、修改时间和品质生成哈希，文件变化后可自然失效。
  - 生成缩略图时使用 Rust `image` 库解码、Lanczos3 缩放，普通图写 JPEG，透明图写 PNG。
  - SVG 素材会被扫描并作为矢量缩略图写入缓存，避免素材库漏掉常见图标文件。
  - 前端生成进度仍逐张更新，但图片显示改为读取 Rust 落盘后的缓存文件。
  - 保留浏览器开发环境的 Canvas 缩略图回退逻辑。
  - 增加 Tauri Dialog 插件与权限配置。
  - 验证前端构建、Rust 编译检查和桌面打包均正常。

- 优化缩略图品质选择与压缩生成：
  - 将“缩略图质量”从独立偏好项移入手动生成缩略图区域。
  - 生成按钮会明确显示本次使用的品质，例如“生成全部（紧凑）”。
  - 缩略图生成改为真实派生缩略图，不再直接复用原图地址。
  - 生成时会按紧凑、标准、高清三档先降尺寸，再编码为更小的 WebP/JPEG/PNG 缩略图。
  - 采用分步降采样，减少一次性大幅缩放带来的模糊和锯齿。
  - 缓存占用改为读取真实缩略图 blob 大小，而不是按固定常量估算。
  - 小体积缓存显示改为 KB，避免压缩后仍显示为 `0.0 MB`。
  - 验证紧凑品质可生成 160px 缩略图，破图数量为 0，缓存统计来自真实生成结果。

- 修复生成缩略图后显示问号：
  - 修复素材状态更新时提前释放本地 `blob:` 图片地址的问题。
  - 预览地址现在只在替换资源库或应用退出时清理，不会在缩略图生成进度更新中失效。
  - 素材墙和右侧预览增加图片加载失败兜底，占位提示替代系统破图图标。
  - 验证生成缩略图后图片可正常加载，破图数量为 0。

- 重做缩略图生成逻辑：
  - 打开资源目录后默认不生成缩略图，素材墙和右侧预览先显示占位图。
  - 缩略图改为设置面板内手动生成，支持生成全部素材。
  - 支持在设置面板中勾选一个或多个文件夹，只生成所选文件夹内的素材缩略图。
  - 支持选择紧凑、标准、高清三种缩略图质量；重复生成会覆盖目标范围内已生成的缩略图状态。
  - 设置面板实时显示已生成、未生成、缓存占用和本次生成进度。
  - 生成过程中显示进度条、当前处理文件和本次完成数量。
  - 清理缓存会将所有缩略图状态重置为未生成，素材墙重新显示占位图。
  - 验证默认占位、全部生成、按文件夹生成、清理缓存和进度显示均正常。

- 完善标签编辑与清理无效入口：
  - 右侧素材信息栏的标签从静态展示改为可交互控件。
  - 支持输入新标签、选择表情插入标签、按 Enter 添加标签。
  - 右侧标签可点击筛选素材，也可单独移除。
  - 标签变更后会同步更新左侧标签栏和顶部标签筛选栏。
  - 移除顶栏左侧应用名称、三条线菜单、右侧头像图标等无效元素。
  - 移除视图区返回/前进按钮、标签栏彩色图标、左侧标签区收藏/搜索/添加等暂无功能的入口。
  - 验证标签添加、表情插入、筛选、删除和无效入口移除均正常。

- 优化偏好设置灰阶视觉：
  - 移除偏好设置中的蓝色高亮与蓝色图标强调。
  - 将分段按钮、打开文件夹按钮、缓存指标、操作按钮、复选框和滑杆统一调整为黑白灰体系。
  - 将配置代码预览从偏蓝文字改为中性灰文字，整体更接近 Photoshop 类专业工具的克制风格。
  - 灰阶样式限定在偏好设置面板内，不影响素材选中态和主界面状态色。

- 新增方向键选择方式设置：
  - 在偏好设置中新增“素材选择 / 方向键切换”选项。
  - 支持在“左右键”和“上下键”两种素材选择方式之间切换。
  - 默认保持左右键切换素材，兼容现有操作习惯。
  - 切换为上下键后，上下方向键负责选择上一张或下一张素材，左右方向键不再改变选择。
  - 所有选择方式继续复用当前视觉顺序和边界滚动逻辑。

- 修复三种视图的键盘导航与滚动：
  - 左右方向键导航改为按当前素材卡片的屏幕视觉位置计算顺序。
  - 瀑布流视图按从左到右、从上到下的视觉顺序切换素材，避免按列优先跳转。
  - 列表视图和自适应视图中，选中项靠近或超出素材区边界时会立即滚动到可见区域。
  - 键盘触发的滚动改为即时滚动，并在下一帧复查一次，提升连续按键和列表末尾场景的稳定性。
  - 验证自适应、列表和瀑布流三种视图下的方向键切换与边界滚动行为。

## 2026-05-25

- 修复列表末尾键盘滚动：
  - 为左右方向键导航增加滚动触发序号，即使已到最后一个素材继续按右键，也会重新检查选中项是否完全可见。
  - 为素材项增加稳定的 `data-asset-id`，滚动逻辑按目标素材精确定位。
  - 将滚动检查放到布局更新后执行，减少列表末尾和快速连续按键时的时序问题。
  - 验证列表视图切换到最后一个素材时，会自动滚动到选中项完整可见。
  - 重新打包并启动最新版 `Picman.app`。

- 优化素材区键盘滚动逻辑：
  - 在素材视图中拦截上下方向键，避免触发浏览器或素材区默认滚动。
  - 左右方向键仍用于切换选中素材。
  - 左右切换后，只有当新选中素材靠近或超出当前可视区域边缘时，素材区才自然滚动。
  - 自适应、瀑布流和列表视图共用同一套边缘滚动逻辑。
  - 验证自适应视图和列表视图中，上下键不滚动，左右键越界时会自动展示选中项。
  - 重新打包并启动最新版 `Picman.app`。

- 优化窗口和左侧文件夹栏：
  - 将 macOS 左上角红黄绿窗口按钮向下调整，使其与顶部文字和图标更协调。
  - 左侧文件夹栏标题由固定“文件夹”改为当前资源库文件夹名称。
  - 文件夹目录树不再重复显示资源库根目录，只展示子目录。
  - 移除文件夹栏无效的添加按钮。
  - 文件夹栏搜索按钮改为真实搜索输入框，可按文件夹名称或路径实时筛选子目录。
  - 验证文件夹搜索、空状态、根目录隐藏和构建流程均正常。
  - 重新打包并启动最新版 `Picman.app`。

- 调整素材显示模式：
  - 移除固定正方形“网格”视图。
  - 将默认显示模式改为“自适应”视图。
  - 保留“瀑布流”视图，并新增“列表”视图。
  - 列表视图改为紧凑行布局，展示缩略图、文件名、路径、尺寸、类型、大小和修改日期。
  - 验证工具栏只保留“自适应 / 瀑布流 / 列表”三种按钮，且默认进入自适应视图。
  - 重新打包并启动最新版 `Picman.app`。

- 修复更换文件夹后三种显示模式失效：
  - 修复真实文件扫描后尺寸字符串使用 `×` 导致瀑布流比例解析失败的问题。
  - 为素材数据补充独立的 `width`、`height` 数值字段，视图布局不再依赖尺寸显示文案。
  - 本地导入 SVG 时优先解析 `width`、`height` 和 `viewBox`，确保 SVG 也能参与瀑布流比例计算。
  - 统一真实图片扫描后的尺寸格式，避免示例素材和本地素材的显示模式行为不一致。
  - 验证三种显示模式切换后仍有明确的缩略图宽高差异。
  - 重新打包并启动最新版 `Picman.app`。

- 重做三种素材显示模式：
  - 将“网格”模式改为固定尺寸的统一正方形缩略图格子，便于快速扫描。
  - 将“瀑布流”模式改为按图片原始宽高比排布，竖图和长图会形成明显错落。
  - 将“自适应”模式改为根据素材区宽度自动计算列数并拉伸卡片宽度。
  - 调整三种模式下的缩略图填充方式，减少模式之间看起来相同的问题。
  - 验证三种模式下的列数、缩略图宽高和竖图高度均有明确差异。
  - 重新打包并启动最新版 `Picman.app`。

- 补齐视图区常用按钮交互：
  - 将顶部右侧“信息栏”按钮接入真实状态，可隐藏或恢复右侧素材信息栏。
  - 将素材区视图按钮改为可切换的三种模式：网格、瀑布流、自适应。
  - 瀑布流视图按素材比例排列，适合浏览不同尺寸图片。
  - 自适应视图会根据可用宽度自动铺满列宽。
  - 将缩略图放大、缩小图标改为真实按钮，支持按 10px 步进调整缩略图尺寸。
  - 验证信息栏切换、三种视图切换、缩略图尺寸调整均可生效。
  - 重新打包并启动最新版 `Picman.app`。

- 修正三栏竖向分割线：
  - 将左侧栏与中间素材区之间的竖向分割线改为单条 1px 线。
  - 将中间素材区与右侧信息栏之间的竖向分割线改为单条 1px 线。
  - 保留 5px 隐形拖动命中区，拖动时仍高亮单线。
  - 验证两条竖向分割线仍可左右拖动。
  - 重新打包并启动最新版 `Picman.app`。

- 修正左侧上下分割线：
  - 将文件夹和标签之间的横向分割线从双边框改为单条 1px 线。
  - 将横向分割线的实际拖动命中区扩大到 10px，视觉仍保持单线。
  - 为横向分割线增加指针捕获，提升拖动稳定性。
  - 验证左侧文件夹和标签区域之间的分割线可上下拖动。
  - 重新打包并启动最新版 `Picman.app`。

- 完善桌面窗口和分割线交互：
  - 顶部栏接入 Tauri 原生窗口拖动能力，鼠标拖动顶部空白区域可移动窗口。
  - 为 Tauri 权限配置增加 `core:window:allow-start-dragging`，确保打包版可使用原生拖窗。
  - 移除左上角 Picman 软件图标，只保留应用名称和主菜单按钮。
  - 将 macOS 红黄绿窗口按钮向内调整，避免过于贴近窗口左上角。
  - 左侧栏与中间工作区之间新增可左右拖动的竖向分割线。
  - 中间素材区与右侧信息栏之间新增可左右拖动的竖向分割线。
  - 左侧栏“文件夹”和“标签”区域之间新增可上下拖动的横向分割线。
  - 为拖动状态增加统一的列/行调整光标和禁止文字选择处理。
  - 重新打包并启动最新版 `Picman.app`。

- 统一日志规则：
  - 新增本文件作为唯一功能更新日志。
  - 合并原 `DEVLOG.md` 与 `CHANGELOG.md` 中的历史记录。
  - 后续不再分开维护开发日志和更新日志。
  - 日志内容统一改为中文，并按日期倒序排列。

- 完成 Billfish 风格界面重构：
  - 参考 Billfish Mac 端公开界面结构，重做整体前端布局与暗色配色。
  - 顶部栏改为“本地资源库 / 云端资源库”资源库标签结构。
  - 将搜索、缩放、排序、刷新、筛选和视图布局按钮集中到中间视图区顶部功能栏。
  - 左侧栏拆成“文件夹”和“标签”两个管理区域。
  - 中间素材区改为更接近 Billfish 的深色画布、紧凑缩略图、文件类型角标、文件名和尺寸信息展示。
  - 右侧栏改为更窄的暗色素材信息面板。
  - 保持现有功能不变，仅调整视觉、布局和交互入口位置。

- 继续完善 Billfish 风格细节：
  - 将可见界面文字中文化，包括筛选弹层、排序菜单、右侧素材信息、状态栏、左侧栏底部和设置面板。
  - 移除筛选弹层旧的内联定位，让弹层位置完全由 CSS 控制。
  - 调高中间工具栏的响应式列宽，避免打包版窄窗口下按钮被右侧栏遮挡。
  - 将 macOS 最小窗口宽度提高到 `1080`，保护三栏工具布局。
  - 重新打包并启动最新版 `Picman.app`。

- 修复素材选中高亮逻辑：
  - 将选中态、键盘焦点态和鼠标悬停态拆分为不同视觉状态。
  - 修复方向键移动选择后旧素材看起来仍有高亮框的问题。
  - 支持按 `Esc` 清空当前选择。
  - 无选中素材时，右侧栏显示空状态。
  - 方向键移动时只保留一个当前选中素材。

- 优化 macOS 顶部栏：
  - 使用 Tauri 的 macOS 覆盖式标题栏。
  - 隐藏原生窗口标题文字。
  - 调整红黄绿窗口按钮位置。
  - 让打包后的顶部原生栏与应用工具栏融合为深色界面。

- 完成 React 结构拆分：
  - 从单体 `App.tsx` 拆出顶部栏、左侧栏、素材网格、右侧信息栏、设置面板、排序菜单、筛选弹层和预览灯箱等组件。
  - 新增共享类型文件 `src/types/library.ts`。
  - 提取素材扫描、文件夹构建、预览地址清理和排序工具函数。
  - 新增 `gif` 和 `avif` 作为可识别素材类型。
  - 为素材缩略图补充键盘可访问性，支持 `Enter` 和空格触发。
  - 修复灯箱缩放状态的 React 静态检查问题。

- 完善桌面工具式布局：
  - 将页面式布局改为桌面软件布局。
  - 固定顶部工具栏、左侧栏和右侧信息栏。
  - 仅中间素材区滚动。
  - 将筛选区域从页面内容改为工具栏弹层，避免推挤素材网格。
  - 移除移动端单列折叠逻辑。
  - 增加 Tauri 最小窗口尺寸限制。
  - 调整标题、网格密度和信息层级，使其更像图片素材管理工具。

- 完成 Eagle 风格视觉过渡版：
  - 曾将最小可用版本视觉调整为 Eagle 类桌面素材管理器风格。
  - 增加暗色左侧导航、紧凑素材网格、右侧信息栏、工具栏筛选和底部状态栏。
  - 后续已继续替换为 Billfish 风格。

- 完善本地素材导入和预览：
  - 支持从应用内打开本地图片文件夹。
  - 支持 PNG、JPG、WEBP、SVG、GIF、AVIF 等图片文件。
  - 使用浏览器对象地址生成本地预览，不修改图片源文件。
  - 根据导入文件动态生成文件夹树和文件夹数量。
  - 顶部筛选控件可按文件类型和缩略图状态筛选素材。
  - 修复窄视口下内容层覆盖顶部按钮的问题。

- 完成 Tauri Mac 桌面壳：
  - 添加 Tauri 2 项目配置和桌面运行脚本。
  - 安装 Rust 工具链：
    - `rustc 1.95.0`
    - `cargo 1.95.0`
    - `stable-aarch64-apple-darwin`
  - 成功运行 `npm run desktop:dev`。
  - 成功运行 `npm run desktop:build`。
  - 生成 macOS 应用和 DMG：
    - `src-tauri/target/release/bundle/macos/Picman.app`
    - `src-tauri/target/release/bundle/dmg/Picman_0.1.0_aarch64.dmg`
  - 验证打包版 macOS 应用可启动。

- 初始化 Picman 最小可用版本：
  - 确定最小可用版本技术栈为 Tauri + React + TypeScript + Vite。
  - 创建 Vite React TypeScript 项目。
  - 添加 lucide-react 图标库。
  - 搭建第一版本地素材管理界面，包括资源库浏览、素材状态、元信息和缩略图缓存概念。
  - 添加 `docs/ARCHITECTURE.md`，明确文件优先的开放资源库结构。
  - 移除 Vite 默认模板中未使用的资源。
  - 配置 ESLint 忽略 `src-tauri/target` 生成目录。
  - 验证 `npm run lint`、`npm run build` 和浏览器预览。
